name: publish

permissions:
  contents: read

on:
  push:
    tags:
      - "v*"

jobs:
  build:
    name: Build VSIX
    runs-on: ubuntu-latest
    outputs:
      vsix-name: ${{ steps.package.outputs.vsix-name }}
    steps:
      - uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22

      - run: corepack enable && pnpm install --frozen-lockfile

      - name: Package VSIX
        id: package
        run: |
          pnpm run package
          VSIX_NAME=$(ls *.vsix)
          echo "vsix-name=${VSIX_NAME}" >> "$GITHUB_OUTPUT"

      - name: Upload VSIX artifact
        uses: actions/upload-artifact@v7
        with:
          name: vsix
          path: "*.vsix"

  deploy:
    name: Deploy to ${{ matrix.name }}
    needs: build
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: VS Marketplace
            pat: VS_MARKETPLACE_TOKEN
            registryUrl: https://marketplace.visualstudio.com
          - name: Open VSX
            pat: OPEN_VSX_TOKEN
    steps:
      - name: Download VSIX artifact
        uses: actions/download-artifact@v8
        with:
          name: vsix

      - name: Publish to ${{ matrix.name }}
        # TODO: bump when HaaLeo/publish-vscode-extension ships a Node 24 build (still node20 on v2/master).
        # Upstream issue: https://github.com/HaaLeo/publish-vscode-extension/issues/76
        # Non-blocking for now: GitHub Actions force-runs node20 actions on node24 during the deprecation window.
        uses: HaaLeo/publish-vscode-extension@v2
        with:
          pat: ${{ secrets[matrix.pat] }}
          registryUrl: ${{ matrix.registryUrl || '' }}
          extensionFile: ${{ needs.build.outputs.vsix-name }}
          skipDuplicate: true

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download VSIX artifact
        uses: actions/download-artifact@v8
        with:
          name: vsix

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          files: "*.vsix"
          generate_release_notes: true
