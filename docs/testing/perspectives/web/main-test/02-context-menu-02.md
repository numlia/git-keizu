# テスト観点表: web/main.ts

> Source: `web/main.ts`
> Generated: 2026-09-25T00:00:00Z
> Language: TypeScript
> Test Framework: Vitest
> Responsibility: context-menu

## S61: stashラベル右クリックの対象解決とスタッシュメニュー表示

> Origin: Feature 059-01 (stash-label-context-menu)
> Added: 2026-09-25
> Status: active
> Supersedes: -
> Signature: `showRefBadgeContextMenu(event: MouseEvent, badge: HTMLElement): void`（行内 `.gitRef` のcontextmenuと `RefOverflowOptions.onRefContextMenu` の共通入口。伝播停止直後のstash分岐）
> Target Path: `web/main.ts`（`showRefBadgeContextMenu` のstash分岐と、`renderTable()` 内の `.gitRef.stash` ラベル生成への `data-stash-hash` 属性付与。実装後に行範囲へ更新）
> Test File: `tests/web/main.refOverflow.test.ts`

行内と折り畳み一覧のstashラベルから、対応する行の既存スタッシュメニューへ到達し、確認ダイアログと要求送信まで同じ対象を使うことを検証する。builder内の項目分岐と確認文言は S2（`tests/web/main.test.ts` TC-006〜TC-016）の責務。実controller・実contextMenu・実dialogsを使い、`buildStashContextMenuItems` / `buildRefContextMenuItems` / `buildDetachedWorktreeContextMenuItems` / `showContextMenu` は実装を呼ぶspyで回数と引数を記録する。`TEST_REPO = "/test/repo"`。

fixture: stash A = `hashOf(11)`・`stash@{2}`、stash B = `hashOf(22)`・`stash@{7}`、通常コミット1件（既存 `commitOf` / `hashOf`。配列indexや表示順とstash番号は一致させない。検証用の識別値であり製品の固定値ではない）。行内では十分な幅、一覧では既存layout fixtureでstashラベルも隠れる幅にし、操作前に可視ラベル／一覧の複製の存在をassertする。「ラベル本体」へのイベントは余白のDOM代替であり、余白の実座標は実画面確認で記録する。以下「元の行」は `tableElem` 内で `dataset.hash` が一致する `.commit` 要素、「stash builderの正常引数」は `(TEST_REPO, hash, selector, 元の行)`、「表示の正常引数」は `showContextMenu` の第2引数がbuilderの戻り値、第3引数が右クリックしたラベル（行内ラベルまたは一覧の複製、`toBe`）、第4引数が現在repoのrecent actions を指す。「無効時の期待」は `showContextMenu` 0回、stash / 参照 / detached worktree の各builder 0回、`postMessage` 0件、windowのerror 0件（例外なし）、参照メニューへのfallbackなし を指す。

| Case ID | Input / Precondition                                                                                                                                                        | Perspective (Normal / Validation / Exception / External / Boundary / Type) | Expected Result                                                                                                                                                                                                       | Notes                                                            |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- |
| TC-494  | stash A の行内ラベル内のSVG `path` をtargetにcontextmenu                                                                                                                    | Normal - 行内アイコンからの表示                                            | stash builderが1回 `(TEST_REPO, hashOf(11), "stash@{2}", Aの元の行)`、表示が1回で正常引数（第3引数はAの行内ラベル）。参照builderは0回                                                                                 | 修正前は参照builderへ流れる（RED対象）。AC 8.1                   |
| TC-495  | 検索で stash A の行内ラベル文字が `span.findMatch` に囲まれ、その `span.findMatch` をtargetにcontextmenu                                                                    | Boundary - 検索マーク内の文字                                              | TC-494と同じ回数・引数（event.targetではなく引数のラベルで判定）。参照builderは0回                                                                                                                                    | RED対象                                                          |
| TC-496  | stash A の行内ラベル本体（`.gitRef.stash` 自身）をtargetにcontextmenu                                                                                                       | Normal - ラベル本体（余白のDOM代替）                                       | TC-494と同じ回数・引数。参照builderは0回                                                                                                                                                                              | RED対象。余白の実座標は実画面で確認                              |
| TC-497  | stash A のラベルが折り畳まれた一覧の複製内のSVG `path` をtargetにcontextmenu（操作前に複製の存在をassert）                                                                  | Normal - 一覧アイコンからの表示                                            | stash builderが1回 `(TEST_REPO, hashOf(11), "stash@{2}", Aの元の行)`（第4引数は複製ではなく `tableElem` 内の元の行、`toBe`）。表示が1回、第3引数は一覧の複製（`toBe`、行内ラベルと非同一）。参照builderは0回          | RED対象。AC 8.1                                                  |
| TC-498  | 検索で一覧の複製内の stash A の文字が `span.findMatch` に囲まれ、その `span.findMatch` をtargetにcontextmenu                                                                | Boundary - 一覧の検索マーク内の文字                                        | TC-497と同じ回数・引数。参照builderは0回                                                                                                                                                                              | RED対象                                                          |
| TC-499  | stash A の一覧の複製本体をtargetにcontextmenu                                                                                                                               | Normal - 一覧の複製本体（余白のDOM代替）                                   | TC-497と同じ回数・引数。参照builderは0回                                                                                                                                                                              | RED対象                                                          |
| TC-500  | stash B の行内ラベル内のSVG `path` をtargetにcontextmenu                                                                                                                    | Normal - 別stashの行内アイコン                                             | stash builderが1回 `(TEST_REPO, hashOf(22), "stash@{7}", Bの元の行)`、表示が1回で正常引数（第3引数はBの行内ラベル）。引数にAのhash・selector・行を含まない。参照builderは0回                                          | RED対象。A/Bの対象分離                                           |
| TC-501  | 検索で stash B の行内ラベル文字が `span.findMatch` に囲まれ、その `span.findMatch` をtargetにcontextmenu                                                                    | Boundary - 別stashの検索マーク内の文字                                     | TC-500と同じ回数・引数。参照builderは0回                                                                                                                                                                              | RED対象                                                          |
| TC-502  | stash B の行内ラベル本体をtargetにcontextmenu                                                                                                                               | Normal - 別stashのラベル本体                                               | TC-500と同じ回数・引数。参照builderは0回                                                                                                                                                                              | RED対象                                                          |
| TC-503  | stash B の一覧の複製内のSVG `path` をtargetにcontextmenu                                                                                                                    | Normal - 別stashの一覧アイコン                                             | stash builderが1回 `(TEST_REPO, hashOf(22), "stash@{7}", Bの元の行)`（第4引数は元の行、`toBe`）。表示が1回、第3引数はBの複製（`toBe`）。Aの値・行・複製を含まない。参照builderは0回                                   | RED対象                                                          |
| TC-504  | 検索で一覧の複製内の stash B の文字が `span.findMatch` に囲まれ、その `span.findMatch` をtargetにcontextmenu                                                                | Boundary - 別stashの一覧の検索マーク内の文字                               | TC-503と同じ回数・引数。参照builderは0回                                                                                                                                                                              | RED対象                                                          |
| TC-505  | stash B の一覧の複製本体をtargetにcontextmenu                                                                                                                               | Normal - 別stashの一覧の複製本体                                           | TC-503と同じ回数・引数。参照builderは0回                                                                                                                                                                              | RED対象                                                          |
| TC-506  | stash A のラベルから `data-stash-hash` 属性を削除してcontextmenu                                                                                                            | Validation - ハッシュ属性の欠落                                            | 無効時の期待（表示・builder・要求0、例外なし）                                                                                                                                                                        | 行内・一覧で各実行。AC 8.3                                       |
| TC-507  | stash A のラベルの `data-stash-hash` を空文字 `""` へ変更してcontextmenu                                                                                                    | Boundary - 空文字の属性                                                    | 無効時の期待                                                                                                                                                                                                          | 行内・一覧で各実行                                               |
| TC-508  | `data-stash-hash` を `commitLookup` に存在しないハッシュ（例 `hashOf(99)`）へ変更してcontextmenu                                                                            | Validation - 未知ハッシュ                                                  | 無効時の期待                                                                                                                                                                                                          | 行内・一覧で各実行                                               |
| TC-509  | `commitLookup[hashOf(11)]` を数値でない値（文字列 `"0"`）に差し替えてcontextmenu                                                                                            | Type - lookup値が非数値                                                    | 無効時の期待（別indexへ丸めない）                                                                                                                                                                                     | 行内・一覧で各実行。lookupの差し替えはコピーを使い、終了時に復元 |
| TC-510  | `commitLookup[hashOf(11)]` は数値だが `commits[index]` が存在しない（配列範囲外）状態でcontextmenu                                                                          | Boundary - 配列範囲外のindex                                               | 無効時の期待                                                                                                                                                                                                          | 行内・一覧で各実行                                               |
| TC-511  | `commitLookup[hashOf(11)]` の先のコミットが別ハッシュ（例 `hashOf(22)`）を持つ状態でcontextmenu                                                                             | Validation - hash不一致                                                    | 無効時の期待（別stash Bを対象にしない）                                                                                                                                                                               | 行内・一覧で各実行                                               |
| TC-512  | lookup先のコミットの `stash` が `null`（通常コミット）の状態でcontextmenu                                                                                                   | Boundary - stashがnull                                                     | 無効時の期待                                                                                                                                                                                                          | 行内・一覧で各実行                                               |
| TC-513  | lookup先のコミットの `stash` が `undefined` の状態でcontextmenu                                                                                                             | Type - stashがundefined                                                    | 無効時の期待                                                                                                                                                                                                          | 行内・一覧で各実行。テスト用の不正入力として明示                 |
| TC-514  | データは有効だが、`tableElem` 内に `dataset.hash === hashOf(11)` の `.commit` 行がない（行削除または `dataset.hash` 書換え）状態でcontextmenu                               | Validation - 元の行の欠落                                                  | 無効時の期待（ハッシュをCSS selectorへ埋め込まずreturn）                                                                                                                                                              | 行内・一覧で各実行                                               |
| TC-515  | stash A と同じ行のworktreeなしlocal branch `feature/x` のラベルでcontextmenu（行内／一覧）                                                                                  | Normal - 同一行のlocal branch                                              | `buildRefContextMenuItems` が1回、第2引数 `feature/x`、第3引数がそのラベル、第4引数 `false`、第7引数 `null`。stash builderは0回                                                                                       | AC 8.3。行がstashでも全ラベルをstash扱いにしない                 |
| TC-516  | 同じ行の結合バッジの `.gitRefHeadRemote`（`data-name="origin/main"`）でcontextmenu（行内／一覧）                                                                            | Normal - 同一行のcombined remote                                           | `buildRefContextMenuItems` が1回、第2引数 `origin/main`、第4引数 `true`。stash builderは0回                                                                                                                           | -                                                                |
| TC-517  | 同じ行のstandalone remote `origin/dev` のラベルでcontextmenu（行内／一覧）                                                                                                  | Normal - 同一行のstandalone remote                                         | `buildRefContextMenuItems` が1回、第2引数 `origin/dev`、第4引数 `false`。stash builderは0回                                                                                                                           | -                                                                |
| TC-518  | 同じ行のtag `v1.0` のラベルでcontextmenu（行内／一覧）                                                                                                                      | Normal - 同一行のtag                                                       | `buildRefContextMenuItems` が1回、第2引数 `v1.0`、第4引数 `false`。stash builderは0回                                                                                                                                 | -                                                                |
| TC-519  | 同じ行のworktree付きbranch（`worktrees.branches` に登録あり）のラベルでcontextmenu（行内／一覧）                                                                            | Normal - 同一行のworktree branch                                           | `buildRefContextMenuItems` が1回、第7引数が `{ path, isMainWorktree }`。stash builderは0回                                                                                                                            | -                                                                |
| TC-520  | 同じ行のブランチなしworktree（`data-worktree-path="/tmp/wt8"`）のラベルでcontextmenu（行内／一覧）                                                                          | Normal - 同一行のdetached worktree                                         | `buildDetachedWorktreeContextMenuItems` が1回 `(TEST_REPO, "/tmp/wt8")`。参照builder・stash builderは0回                                                                                                              | -                                                                |
| TC-521  | stash A の行本体／行内ラベル／一覧の複製から実contextMenuを開き、`.contextMenuParent` のmouseenterでMoreを開く                                                              | Normal - 項目構成の一致                                                    | 3経路とも `#contextMenu` の項目が Apply Stash…、Pop Stash…、区切り、More、区切り、Copy Stash Name to Clipboard、Copy Stash Hash to Clipboard の7要素で同順。Moreの子が Create Branch from Stash…、Drop Stash… の2項目 | 各経路で表示は1回                                                |
| TC-522  | 行本体／行内ラベル／一覧の複製から Apply Stash… を選び、`#dialogInput0` のcheckboxが初期 `false` であることを確認してそのまま `#dialogAction`                               | Normal - Apply（Reinstate Index off）                                      | `postMessage` に `{ command: "applyStash", repo: TEST_REPO, selector, reinstateIndex: false }` が1件（A/Bそれぞれのselector）。行本体からの結果と `toEqual` で一致                                                    | A/B × 3経路。§3.5 payload                                        |
| TC-523  | 同じ3経路から Apply Stash… を選び、checkboxを `true` にして `#dialogAction`                                                                                                 | Normal - Apply（Reinstate Index on）                                       | `{ command: "applyStash", repo: TEST_REPO, selector, reinstateIndex: true }` が1件。行本体からの結果と一致                                                                                                            | A/B × 3経路                                                      |
| TC-524  | 同じ3経路から Pop Stash… を選び、checkbox初期 `false` のまま `#dialogAction`                                                                                                | Normal - Pop（Reinstate Index off）                                        | `{ command: "popStash", repo: TEST_REPO, selector, reinstateIndex: false }` が1件。行本体からの結果と一致                                                                                                             | A/B × 3経路                                                      |
| TC-525  | 同じ3経路から Pop Stash… を選び、checkboxを `true` にして `#dialogAction`                                                                                                   | Normal - Pop（Reinstate Index on）                                         | `{ command: "popStash", repo: TEST_REPO, selector, reinstateIndex: true }` が1件。行本体からの結果と一致                                                                                                              | A/B × 3経路                                                      |
| TC-526  | 同じ3経路から More → Create Branch from Stash… を選び、`#dialogInput0` の初期値が `""` であることを確認して `from-stash-test` を入力し `#dialogAction`                      | Normal - Create Branch                                                     | 初期入力が空。`{ command: "branchFromStash", repo: TEST_REPO, branchName: "from-stash-test", selector }` が1件。行本体からの結果と一致                                                                                | A/B × 3経路                                                      |
| TC-527  | 同じ3経路から More → Drop Stash… を選び、削除確認ダイアログで `#dialogAction`                                                                                               | Normal - Drop                                                              | 削除確認ダイアログが表示され、`{ command: "dropStash", repo: TEST_REPO, selector }` が1件。行本体からの結果と一致                                                                                                     | A/B × 3経路                                                      |
| TC-528  | 同じ3経路から Copy Stash Name to Clipboard を選ぶ                                                                                                                           | Normal - Copy Name                                                         | `{ command: "copyToClipboard", type: "Stash Name", data: selector }` が1件。ダイアログは開かない                                                                                                                      | A/B × 3経路                                                      |
| TC-529  | 同じ3経路から Copy Stash Hash to Clipboard を選ぶ                                                                                                                           | Normal - Copy Hash                                                         | `{ command: "copyToClipboard", type: "Stash Hash", data: hash }` が1件（A/Bそれぞれのhash）。ダイアログは開かない                                                                                                     | A/B × 3経路                                                      |
| TC-530  | 一覧の複製から実メニューを開き、Moreをmouseenterで開いた後、項目を選ばずにbody clickで一覧を閉じる                                                                          | Normal - 一覧閉鎖でメニュー消去                                            | メニュー・サブメニュー操作中は一覧要素が存在。閉鎖後は `#contextMenu` とサブメニューが消え（複製の `contextMenuActive` 経由）、`postMessage` 0件                                                                      | AC 8.1                                                           |
| TC-531  | 一覧の複製からメニューを開いた後、実FindWidgetの検索更新（`SEARCH_DEBOUNCE_MS` 待機）で複製を再生成する                                                                     | Normal - 複製再生成でメニュー消去                                          | 古い複製がDOMから外れてメニューが閉じ、新しい複製は古い複製と非同一。`postMessage` 0件                                                                                                                                | 製品へ固定delayを追加しない                                      |
| TC-532  | 一覧の複製から Apply Stash… の確認を開き、一覧をbody clickで閉じてから `#dialogAction`                                                                                      | Normal - 一覧閉鎖後の確定（Apply）                                         | ダイアログ表示元の元の行に `dialogActive` が付き複製には付かない。閉鎖後も確定でき、生成時のrepo/selectorで applyStash（`reinstateIndex: false`）が1件                                                                | AC 8.1                                                           |
| TC-533  | 一覧の複製から Pop Stash… の確認を開き、一覧を閉じてから `#dialogAction`                                                                                                    | Normal - 一覧閉鎖後の確定（Pop）                                           | TC-532と同じ `dialogActive` の位置。生成時の値で popStash（`reinstateIndex: false`）が1件                                                                                                                             | -                                                                |
| TC-534  | 一覧の複製から More → Create Branch from Stash… の入力を開き、一覧を閉じてから `from-stash-test` を入力して `#dialogAction`                                                 | Normal - 一覧閉鎖後の確定（Create Branch）                                 | TC-532と同じ `dialogActive` の位置。生成時の値で branchFromStash（`branchName: "from-stash-test"`）が1件                                                                                                              | -                                                                |
| TC-535  | 一覧の複製から More → Drop Stash… の確認を開き、一覧を閉じてから `#dialogAction`                                                                                            | Normal - 一覧閉鎖後の確定（Drop）                                          | TC-532と同じ `dialogActive` の位置。生成時の値で dropStash が1件                                                                                                                                                      | -                                                                |
| TC-536  | TC-532〜TC-535の4操作で確認を開き、一覧を閉じてから `#dialogDismiss` で取消                                                                                                 | Normal - 一覧閉鎖後の取消                                                  | 各操作で `postMessage` 0件。ダイアログが閉じ、元の行の `dialogActive` が解除                                                                                                                                          | 4操作それぞれ                                                    |
| TC-537  | stash A の行内ラベルを左クリック（click）                                                                                                                                   | Normal - 行内ラベルの左クリック維持                                        | 行のclickへ伝播せず、`commitDetails` 要求なし。`postMessage` 0件、メニュー表示0回                                                                                                                                     | AC 8.2                                                           |
| TC-538  | stash A の行内ラベルをdblclick（既存メニューまたはダイアログを開いた状態から）                                                                                              | Normal - 行内ラベルのダブルクリック維持                                    | 開いていたメニュー・ダイアログが閉じる。checkout要求（`checkoutBranchAction` 実装呼出し経由の `postMessage`）は0件                                                                                                    | checkout stubはこのテスト内だけ実装呼出しへ切替                  |
| TC-539  | stash A の一覧の複製を左クリック（click）                                                                                                                                   | Normal - 一覧ラベルの左クリック維持                                        | 一覧内で伝播停止。`commitDetails` 要求なし、`postMessage` 0件                                                                                                                                                         | -                                                                |
| TC-540  | stash A の一覧の複製をdblclick                                                                                                                                              | Normal - 一覧ラベルのダブルクリック維持                                    | 一覧内で伝播停止。checkout・`commitDetails` 要求なし、`postMessage` 0件                                                                                                                                               | -                                                                |
| TC-541  | stash A の行のラベル以外（メッセージ列など）を左クリック                                                                                                                    | Normal - 行本体の左クリック維持                                            | `postMessage` に `command: "commitDetails"`、対象 `hashOf(11)`、`isStash: true` の要求が1件                                                                                                                           | AC 8.2                                                           |
| TC-542  | stash A の行のラベル以外へdblclickイベントだけを送る（clickを送らない）                                                                                                     | Boundary - 行本体dblclickの既存挙動                                        | 専用dblclickハンドラーがない既存挙動を維持し、追加要求0件                                                                                                                                                             | -                                                                |
| TC-543  | 通常コミット行を右クリック                                                                                                                                                  | Normal - 通常コミットメニュー維持                                          | 通常commitメニューが表示され、stash builderは0回                                                                                                                                                                      | AC 8.2                                                           |
| TC-544  | stash A の行のラベル以外を右クリック                                                                                                                                        | Normal - 行本体からのstashメニュー維持                                     | stash builderが1回 `(TEST_REPO, hashOf(11), "stash@{2}", 元の行)`、表示が1回で第3引数が元の行（`toBe`）                                                                                                               | AC 8.2                                                           |
| TC-545  | 描画後・メニュー生成前に `commits` 内の stash A の `selector` を `stash@{3}` へ差し替え（同じhash）、ラベルからメニューを開いた後にさらに `stash@{4}` へ変更し、Applyを確定 | Boundary - 値の確定時点                                                    | builder第3引数は生成時の現在値 `stash@{3}`（描画時の表示文字 `@{2}` から再取得しない）。確認後の applyStash の selector も `stash@{3}`（生成後の変更 `stash@{4}` を反映しない）                                       | `commits` の差し替えはコピーを使い、終了時に復元                 |

### 失敗源インベントリ（include-or-justify）— Feature 059-01 追加分（S61）

| 失敗源                                                      | 対応ケースまたは除外理由                                                           |
| ----------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| 無効な識別子（属性欠落・空・未知ハッシュ）                  | TC-506、TC-507、TC-508                                                             |
| 古い・壊れたlookup（非数値・範囲外・hash不一致・stash欠落） | TC-509、TC-510、TC-511、TC-512、TC-513                                             |
| 元の行の欠落                                                | TC-514                                                                             |
| 異なるhash/selectorの対象化・値の取得時点                   | TC-500〜TC-505、TC-511、TC-545                                                     |
| 複製と元の行の混同（表示元・確認元）                        | TC-497〜TC-499、TC-503〜TC-505、TC-532〜TC-535                                     |
| 二重表示・二重呼出し                                        | TC-494〜TC-505、TC-521、TC-544                                                     |
| 一覧閉鎖・複製再生成時の寿命                                | TC-530、TC-531、TC-536                                                             |
| 同一行の非stashラベルの誤振分け                             | TC-515〜TC-520                                                                     |
| 既存click / dblclick / 行イベントの回帰                     | TC-537〜TC-543                                                                     |
| 操作payload・確認入力の変化                                 | TC-522〜TC-529                                                                     |
| builder内の項目分岐・確認文言                               | excluded(S2 TC-006〜TC-016 と `tests/web/main.test.ts` の責務)                     |
| 外部Git操作後のselector変化の再検証                         | excluded(対応プラン §7 の対象外。既存行メニューと同じ限界を維持)                   |
| 外部依存・例外                                              | excluded(Git実行・ホスト応答は `postMessage` 境界のpayload比較まで。throw経路なし) |

**失敗カテゴリ網羅（diversity floor）**:

- Validation: TC-506、TC-508、TC-511、TC-514
- Exception: excluded(throw経路なし。各guardは例外なしをwindowのerror 0件で確認)
- External: excluded(上表のとおり)
- Boundary: TC-495、TC-498、TC-501、TC-504、TC-507、TC-510、TC-512、TC-542、TC-545
- Type: TC-509、TC-513

**失敗系/正常系比（煙感知器）**: 正常系37件、失敗系15件。入口の対象解決を追加するため、無効識別子・lookup破損・行欠落の各guardを個別に検証した。

### Feature 059-01 テスト対応と実行証跡（S61）

- テストファイル: `tests/web/main.refOverflow.test.ts`。describe構成の予定は対応プラン Task 3 のとおり（`stash label routing (S61)`、`stash label operations (S61)`、`stash label lifecycle and clicks (S61)`）。実test名と結果は Task 3 で記入する。
- TC-506〜TC-514 は行内・一覧でそれぞれ実行し、TC-522〜TC-529 は A/B × 行本体・行内ラベル・一覧の複製で実行する。parameterized testは入力と期待値を名前に含める。

対応表の先頭列はdescribeとし、Case IDは2列目に置く（派生索引の集計は先頭セルが `TC-` の行をケースとして数えるため）。

| describe（予定）                         | Case ID | Test Method       | 結果              |
| ---------------------------------------- | ------- | ----------------- | ----------------- |
| `stash label routing (S61)`              | TC-494  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-495  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-496  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-497  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-498  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-499  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-500  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-501  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-502  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-503  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-504  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-505  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-506  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-507  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-508  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-509  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-510  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-511  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-512  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-513  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-514  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-515  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-516  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-517  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-518  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-519  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-520  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label operations (S61)`           | TC-521  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label operations (S61)`           | TC-522  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label operations (S61)`           | TC-523  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label operations (S61)`           | TC-524  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label operations (S61)`           | TC-525  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label operations (S61)`           | TC-526  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label operations (S61)`           | TC-527  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label operations (S61)`           | TC-528  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label operations (S61)`           | TC-529  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label lifecycle and clicks (S61)` | TC-530  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label lifecycle and clicks (S61)` | TC-531  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label lifecycle and clicks (S61)` | TC-532  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label lifecycle and clicks (S61)` | TC-533  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label lifecycle and clicks (S61)` | TC-534  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label lifecycle and clicks (S61)` | TC-535  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label lifecycle and clicks (S61)` | TC-536  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label lifecycle and clicks (S61)` | TC-537  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label lifecycle and clicks (S61)` | TC-538  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label lifecycle and clicks (S61)` | TC-539  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label lifecycle and clicks (S61)` | TC-540  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label lifecycle and clicks (S61)` | TC-541  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label lifecycle and clicks (S61)` | TC-542  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-543  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-544  | （Task 3 で記入） | （Task 3 で記入） |
| `stash label routing (S61)`              | TC-545  | （Task 3 で記入） | （Task 3 で記入） |

- 旧実装でのRED確認（TC-494〜TC-505）: （Task 3 で記録。未実施）
- 実画面確認（VS Code、行内・一覧のアイコン／文字／余白の右クリック、More操作、一覧閉鎖、確認の取消、左クリック・ダブルクリック）: （Task 3 で記録。未実施）
