# テスト観点表: web/main.ts

> Source: `web/main.ts`
> Generated: 2026-09-08T00:00:00Z
> Language: TypeScript
> Test Framework: Vitest
> Responsibility: file-history

## S50: FileHistoryController の callback 配線と hook 呼出（render / repo 切替 / Find / 行 click / context menu）

> Origin: Feature 055-07 (light-spec-plan)
> Added: 2026-09-08
> Updated: 2026-09-12
> Status: active
> Supersedes: -
> Signature: `GitKeizuView` constructor の `new FileHistoryController(callbacks)` / `render()` / repo dropdown change・`selectRepo()`・`loadRepos()` / private `openFindWidget(): void` / `.commit` click handler / private `buildFileHistoryMenuContext(): FileHistoryMenuContext` / `public loadFileHistory(response: ResponseFileHistory): void`
> Target Path: `web/main.ts`（constructor・`render()`・`selectRepo()`・`loadRepos()`・`openFindWidget()`・click handler・`buildFileHistoryMenuContext()`。実装後に行範囲へ更新）
> Test File: `tests/web/main.test.ts`

`../../web/fileHistory` を `vi.mock` して controller の method 呼出を観測する配線の観点（対応プラン §4 Task 7 実装内容 2〜8、11）。状態機械の分岐は `web/fileHistory-test.md`、menu item の表示条件は `web/fileMenu-test.md` S4 の責務で本表には含めない。

| Case ID | Input / Precondition                                                   | Perspective (Normal / Validation / Exception / External / Boundary / Type) | Expected Result                                                                                                                                                                                                                                                                                            | Notes                               |
| ------- | ---------------------------------------------------------------------- | -------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- |
| TC-316  | `GitKeizuView` を初期化する                                            | Normal - controller の生成                                                 | `FileHistoryController` の constructor が 1 回呼ばれ、引数 object が `getCommits` / `getCommitId` / `getCurrentRepo` / `getExpandedCommit` / `getScrollTop` / `setScrollTop` / `hideCommitDetails` / `restoreExpandedCommit` / `scrollToCommit` / `closeFindWidget` / `setGraphHighlight` の 11 関数を持つ | `findWidget` 生成直後               |
| TC-317  | constructor に渡された `getCommitId` を既知 hash と未知 hash で呼ぶ    | Normal - getCommitId callback                                              | 既知 hash で `commitLookup` の index（`number`）、未知 hash で `null`（`undefined` ではない）を返す                                                                                                                                                                                                        | `typeof === "number"` 判定          |
| TC-318  | 渡された `setGraphHighlight(highlight)` を呼ぶ                         | Normal - graph callback の配線                                             | `graph.setFileHistoryHighlight` が同じ `highlight` で 1 回呼ばれ、続けて graph の再描画（`graph.render`）が 1 回起きる                                                                                                                                                                                     | `renderGraph()` 経由                |
| TC-319  | `loadCommits` response を受けて `render()` が完了する                  | Normal - render 後の hook                                                  | `onCommitsRendered` が 1 回呼ばれ、その呼出が `renderTable()` による `.commit` 行の再生成より後である                                                                                                                                                                                                      | `render()` の末尾                   |
| TC-320  | `renderShowLoading()` だけが呼ばれる                                   | Boundary - loading 描画                                                    | `onCommitsRendered` の call count が 0                                                                                                                                                                                                                                                                     | `renderShowLoading()` では呼ばない  |
| TC-321  | repo dropdown の change で別 repo を選ぶ                               | Normal - dropdown 切替 hook                                                | `onRepositoryChanged` が 1 回呼ばれ、その呼出が `loadCommits` request の `postMessage` より先である                                                                                                                                                                                                        | `currentRepo` 更新の直前            |
| TC-322  | `selectRepo(<別 repo>)` を呼ぶ                                         | Normal - selectRepo hook                                                   | `onRepositoryChanged` が 1 回呼ばれる                                                                                                                                                                                                                                                                      | -                                   |
| TC-349  | `loadRepos()` が現在の repo を含まない repo set を受け取る             | Normal - repo 消失時の hook                                                | `onRepositoryChanged` が 1 回呼ばれ、その呼出が `refresh("hard")` の `loadBranches` request の `postMessage` より先である                                                                                                                                                                                  | `currentRepo` 置換の直前            |
| TC-323  | `isActive()` が `true` を返す状態で `#searchBtn` を click              | Normal - Find 起動時の解除                                                 | `exit(true)` が 1 回呼ばれ、`mock.invocationCallOrder` で `findWidget.show(true)` より先である                                                                                                                                                                                                             | `openFindWidget()`                  |
| TC-324  | `isPending()` だけが `true` の状態で find keybinding の keydown        | Normal - pending 中の Find 起動                                            | `exit(true)` が 1 回、`findWidget.show(true)` が 1 回呼ばれる                                                                                                                                                                                                                                              | 2 つ目の呼出箇所                    |
| TC-325  | `isActive()` / `isPending()` がともに `false` で `#searchBtn` を click | Boundary - mode 外の Find                                                  | `exit` の call count が 0 で `findWidget.show(true)` が 1 回呼ばれる                                                                                                                                                                                                                                       | 既存挙動維持                        |
| TC-326  | `.commit` 行（通常 commit）を click                                    | Normal - 行 click の先行 hook                                              | `handleCommitRowClick(<hash>)` が 1 回呼ばれ、既存の `commitDetails` request の `postMessage` より先である                                                                                                                                                                                                 | 既存分岐はそのまま続く              |
| TC-327  | uncommitted 行（`hash === "*"`）を click                               | Boundary - uncommitted 行                                                  | `handleCommitRowClick` の call count が 0                                                                                                                                                                                                                                                                  | 対象外の handler                    |
| TC-328  | 通常 commit の CDV で `.gitFile` を右クリック                          | Normal - context 引数の形                                                  | `buildFileContextMenuItems` の第 4 引数が `{ isStash: false, onHighlightFileHistory: <function> }` の形で、第 2 引数が `this.expandedCommit`、第 3 引数が `currentRepo`                                                                                                                                    | -                                   |
| TC-329  | stash commit（`stash !== null`）の CDV で `.gitFile` を右クリック      | Normal - stash 判定                                                        | 第 4 引数の `isStash` が `true`                                                                                                                                                                                                                                                                            | `commits[commitLookup[hash]].stash` |
| TC-330  | TC-328 の `onHighlightFileHistory("h", "p")` を呼ぶ                    | Normal - request への接続                                                  | `fileHistory.request("h", "p")` が 1 回呼ばれる                                                                                                                                                                                                                                                            | Task 4 の stub を置き換え           |
| TC-331  | `loadFileHistory(response)` を呼ぶ                                     | Normal - API の委譲                                                        | `handleResponse` が同じ `response` object（`toBe`）で 1 回呼ばれる                                                                                                                                                                                                                                         | messageHandler からの入口           |

## S51: restoreExpandedCommit() の DOM 再解決と applyFileHistoryToFileRows() のファイル行強調・注記

> Origin: Feature 055-07 (light-spec-plan)
> Added: 2026-09-08
> Status: active
> Supersedes: -
> Signature: private `restoreExpandedCommit(snapshot: FileHistoryExpandedSnapshot): boolean` / private `applyFileHistoryToFileRows(): void`
> Target Path: `web/main.ts`（`restoreExpandedCommit()`・`applyFileHistoryToFileRows()`・`showCommitDetails()` / `handleFileViewToggle()` の bind 直後。実装後に行範囲へ更新）
> Test File: `tests/web/main.test.ts`

controller は mock のまま、`restoreExpandedCommit` と `applyFileHistoryToFileRows` は実 DOM で検証する（対応プラン §4 Task 7 実装内容 9〜10）。`isActive` / `getHistoricalPathFor` の戻り値は mock で制御する。

| Case ID | Input / Precondition                                                                                                                                                     | Perspective (Normal / Validation / Exception / External / Boundary / Type) | Expected Result                                                                                                                                                                                      | Notes                      |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- |
| TC-332  | `.commit[data-hash="e1"]` が存在する状態で `{ hash: "e1", compareWithHash: null, commitDetails, fileTree }` を復元                                                       | Normal - DOM 再解決と詳細再表示                                            | 戻り値が `true`、`#commitDetails` が当該行の直後に生成され、`expandedCommit.srcElem` が現在の DOM 要素（`toBe`）、`expandedCommit.commitDetails` / `fileTree` が snapshot の値、`loading` が `false` | `showCommitDetails()` 経由 |
| TC-333  | `compareWithHash: "c1"` で比較行 `.commit[data-hash="c1"]` も存在                                                                                                        | Normal - 比較行の再解決                                                    | 比較行に `compareTarget` class が付き、`expandedCommit.compareWithSrcElem` が当該 DOM 要素                                                                                                           | -                          |
| TC-334  | `compareWithHash: "c1"` だが比較行が存在しない                                                                                                                           | Boundary - 比較行の unload                                                 | 戻り値が `true` で `expandedCommit.compareWithSrcElem` が `null`、`compareTarget` class を持つ行が 0 件                                                                                              | 詳細だけ復元               |
| TC-335  | `.commit[data-hash="e1"]` が存在しない                                                                                                                                   | Validation - 行の不在                                                      | 戻り値が `false` で `#commitDetails` が生成されず、`expandedCommit` が呼出前と同じ                                                                                                                   | DOM 不変                   |
| TC-336  | `isActive()` が `true`、`expandedCommit.compareWithHash === null`、`getHistoricalPathFor(hash)` が `"src/a.txt"`、CDV に `.gitFile[data-newfilepath="src%2Fa.txt"]` あり | Normal - 一致行の強調                                                      | 当該 `.gitFile` に `fileHistoryCurrent` が付き、`.fileHistoryNote` が 0 件                                                                                                                           | decode 後に照合            |
| TC-337  | 同条件で CDV に一致行がない（CE4 の merge）                                                                                                                              | Normal - 注記の挿入                                                        | `.fileHistoryNote` が `fileViewToggle` の直後に 1 件あり、`textContent` が `"This file's change is not part of the diff against the first parent."`、`.gitFile.fileHistoryCurrent` が 0 件           | 第 1 親 diff との契約差    |
| TC-338  | TC-336 / TC-337 の状態で `handleFileViewToggle()` を実行                                                                                                                 | Normal - view toggle 後の再適用                                            | toggle 後も一致行に `fileHistoryCurrent` が付き直し、注記は 1 件のまま（重複 0）                                                                                                                     | 再 bind 直後に呼ぶ         |
| TC-339  | `getHistoricalPathFor()` が `null`                                                                                                                                       | Validation - 非 match commit の CDV                                        | `fileHistoryCurrent` を持つ `.gitFile` と `.fileHistoryNote` がともに 0 件                                                                                                                           | -                          |
| TC-340  | `expandedCommit.compareWithHash !== null`（比較 mode）                                                                                                                   | Validation - 比較 mode                                                     | `fileHistoryCurrent` を持つ `.gitFile` と `.fileHistoryNote` がともに 0 件                                                                                                                           | -                          |
| TC-341  | `isActive()` が `false`                                                                                                                                                  | Boundary - inactive                                                        | `fileHistoryCurrent` を持つ `.gitFile` と `.fileHistoryNote` がともに 0 件                                                                                                                           | no-op                      |

### 失敗源インベントリ（include-or-justify）— Feature 055-07 追加分（S50〜S51）

| 失敗源                                                              | 対応ケースまたは除外理由                                                                                                                               |
| ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| hook の未接続・順序逆転（render / repo 切替 / Find / 行 click）     | TC-319、TC-321〜TC-324、TC-326、TC-349                                                                                                                 |
| hook の過剰呼出（loading 描画 / uncommitted 行 / mode 外）          | TC-320、TC-325、TC-327                                                                                                                                 |
| callback の誤配線（`getCommitId` の `undefined`、graph 再描画漏れ） | TC-317、TC-318                                                                                                                                         |
| stash 判定・context 引数の欠落                                      | TC-328、TC-329                                                                                                                                         |
| `onHighlightFileHistory` が stub のまま残る                         | TC-330                                                                                                                                                 |
| response の委譲漏れ                                                 | TC-331                                                                                                                                                 |
| DOM 再生成後の旧 `srcElem` 使用・行不在での復元                     | TC-332、TC-335                                                                                                                                         |
| 比較行の unload・`compareTarget` 漏れ                               | TC-333、TC-334                                                                                                                                         |
| 一致行の取り違え（encode 済み path との直接比較）・注記の重複       | TC-336〜TC-338                                                                                                                                         |
| 非 match / 比較 mode / inactive での強調・注記                      | TC-339〜TC-341                                                                                                                                         |
| 境界値（0 / minimum / maximum / +/-1 / empty / NULL）               | 0 回呼出: TC-320、TC-325、TC-327。`null` 戻り: TC-317、TC-339。比較行 `null`: TC-334。inactive: TC-341。maximum / +/-1: excluded(数値閾値が存在しない) |
| 外部依存×失敗モード                                                 | excluded(controller は mock で、Git 失敗は `web/fileHistory-test.md` S3 が response 経由で扱う)                                                        |
| 例外・エラー経路                                                    | excluded(main.ts の hook は throw 分岐を持たない)                                                                                                      |
| stale response / anchor 不在 / unload の判定                        | excluded(`web/fileHistory-test.md` S3、S5、S8 の責務。main.ts は callback を提供するだけ)                                                              |

**失敗カテゴリ網羅（diversity floor）**:

- Validation: TC-335、TC-339、TC-340
- Exception: excluded(上表のとおり throw 経路なし)
- External: excluded(上表のとおり)
- Boundary: TC-320、TC-325、TC-327、TC-334、TC-341
- Type: excluded(引数の型は `src/types-test.md` S9 と TypeScript の型検査で担保)
- Normal: TC-316〜TC-319、TC-321〜TC-324、TC-326、TC-328〜TC-333、TC-336〜TC-338、TC-349

**失敗系/正常系比（煙感知器）**: 正常系19件、失敗系8件。配線の観点は「呼ばれること」の正常系が構造的に多く、失敗源は未接続・過剰呼出・DOM 不在・mode 外に限られることを上表で列挙した。比率合わせのためのケース追加・削除は行わない。
