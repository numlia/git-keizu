# テスト観点表: web/main.ts

> Source: `web/main.ts`
> Generated: 2026-09-24T00:00:00Z
> Language: TypeScript
> Test Framework: Vitest
> Responsibility: rendering

## S60: refバッジ折り畳みの描画接続と説明列の最小幅（全件収容行の最小幅を訂正）

> Origin: Feature 059-02 (light-spec-plan)
> Added: 2026-09-24
> Status: active
> Supersedes: S56
> Signature: `private renderTable(): void` / `private makeTableResizable(): void` / `private observeWindowSizeChanges(): void` と `RefOverflowController` の生成・`attachTable`・`onMinimumWidth` の接続、`FindWidgetCallbacks.onHighlightsChanged` の配線
> Target Path: `web/main.ts:287-303`（controller生成と `onHighlightsChanged` の配線）、`web/main.ts:1070-1072`（`renderTable()` 末尾のattach）、`web/main.ts:1133-1238`（`makeTableResizable()` の列幅・ドラッグ）、`web/main.ts:1268-1326`（ドラッグ下限・最小幅の適用・表示用固定幅）、`web/main.ts:1329-1349`（`observeWindowSizeChanges()`）
> Test File: `tests/web/main.refOverflow.test.ts`

S56の描画接続・最小幅・保存値維持の契約を引き継ぎ、S56 TC-430 の期待結果だけを訂正する。S56 TC-430 は「バッジを持たない行と全件がBに収まる行だけの表」で `min-width` を空文字（`onMinimumWidth(null)`）としていたが、対応プラン §3.4「全バッジが収まる場合も最小幅は自然幅のカウンター候補から算出する」と §3.5（`null` の通知はバッジのある対象行がない場合と detach のみ）により、全件収容行を含む表には数値の最小幅が適用される（`web/refOverflow-test.md` TC-046 と同じ契約）。このためバッジなし行だけの表（TC-478）と全件収容行を含む表（TC-479）に分割した。

実main描画に寸法モック（`getBoundingClientRect`・`getComputedStyle`・ResizeObserver・rAF）を与え、controllerの生成・attach・最小幅の適用・保存値の維持を検証する。幅判定の全分岐は `web/refOverflow-test.md` S1〜S3 の責務。最小幅の適用fixtureは、他4列の実使用外幅を40 / 80 / 120 / 90（計330）、表のborder差分を0とする。`TEST_REPO = "/test/repo"`。

| Case ID | Input / Precondition                                                                                                                                                                                      | Perspective (Normal / Validation / Exception / External / Boundary / Type) | Expected Result                                                                                                                                                                                                                | Notes                                                                                                                                                               |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| TC-475  | `gitBranchHead="main"`、1コミットに `main`（remotes `["origin"]`）と長い名前のlinked worktree branch 5個、W=700・外幅 `[96.4,232.8,284.1,232.8,284.1,232.8]`・counter外幅30.7の寸法モックで `loadCommits` | Normal - 主回帰                                                            | 当該行の `.refOverflowCounter` が1個で文字列 `+4`。結合バッジと1個目のworktreeが可視、残り4個のworktreeが元順のまま `.refOverflowHidden` を持つ                                                                                | AC-01。修正前コミットではcounterが存在せず失敗する（RED証跡はTask 6）。S56/TC-427 の引き継ぎ                                                                        |
| TC-476  | TC-475の描画後、計測領域 `.refOverflowMeasure` 内の `.gitRef` 複製でcontextmenuとdblclickを発火                                                                                                           | Validation - 計測用複製を既存リスナーが拾わない                            | `showContextMenu` の呼出し0回、checkoutの `postMessage` 0回                                                                                                                                                                    | 元refのリスナー登録完了後にattachする（Task 3 実装内容2）。S56/TC-428 の引き継ぎ                                                                                    |
| TC-477  | TC-475の描画後、可視の結合バッジ本体でdblclick                                                                                                                                                            | Normal - 行内dblclickの維持                                                | checkoutが従来どおり生のref名 `main` で1回実行される                                                                                                                                                                           | AC-14。S56/TC-429 の引き継ぎ                                                                                                                                        |
| TC-478  | バッジを持たない行だけの表を描画                                                                                                                                                                          | Boundary - 対象行なし                                                      | 各行の説明セルのrefマークアップが従来どおりで `.refOverflowCounter` が0個。`#content` と表のinline `min-width` が空文字                                                                                                        | onMinimumWidth(null)の経路（§3.5）。AC-13。S56/TC-430 のうちバッジなし表の分を引き継ぎ                                                                              |
| TC-479  | バッジ2個（`main`、`dev`）がすべてBに収まる行と、バッジを持たない行の表を描画                                                                                                                             | Boundary - 全件収容                                                        | 全件収容行の `.refOverflowCounter` が0個、`.refOverflowHidden` が0個で、バッジが元順（`main`、`dev`）のまま表示される。表のinline `min-width` は空文字ではなく、自然幅のcounter候補から求めた数値（fixtureでは `405px`）になる | §3.4「全バッジが収まる場合も最小幅は自然幅のカウンター候補から算出する」。`web/refOverflow-test.md` TC-046 と同じ契約。AC-13。S56/TC-430 のうち全件収容行の分を訂正 |
| TC-480  | 最小幅通知が `82`                                                                                                                                                                                         | Normal - 表と共通領域の最小幅                                              | `#content` と表のinline `min-width` がともに `412px`（他4列330＋M82＋border差分0）                                                                                                                                             | AC-11、AC-12。S56/TC-431 の引き継ぎ                                                                                                                                 |
| TC-481  | 最小幅通知が有効値 `64`、別途 `null`                                                                                                                                                                      | Boundary - 64とnullの区別                                                  | `64` では `#content` と表の `min-width` が `394px`。`null` では両方とも空文字になり、ドラッグの下限は既存のヘッダー64pxへ戻る                                                                                                  | §3.5。S56/TC-432 の引き継ぎ                                                                                                                                         |
| TC-482  | TC-480の適用後、説明セル幅0で計測不能になり通知が来ない                                                                                                                                                   | Validation - 計測不能時の現状維持                                          | `#content` と表の `min-width` が `412px` のまま                                                                                                                                                                                | §3.5。S56/TC-433 の引き継ぎ                                                                                                                                         |
| TC-483  | M=82、説明セルの現在内幅120で、graph列と説明列の境界（case 0）を右へ100pxドラッグ                                                                                                                         | Boundary - case 0 の下限                                                   | ドラッグのdeltaが38pxへ抑止され、`columnWidths[0]` の増分が38                                                                                                                                                                  | 説明セル内幅は82で止まる。AC-11。S56/TC-434 の引き継ぎ                                                                                                              |
| TC-484  | M=82、説明セルの現在内幅120で、説明列と日付列の境界（case 1）を左へ100pxドラッグ                                                                                                                          | Boundary - case 1 の下限                                                   | deltaが−38pxへ抑止され、`columnWidths[1]` の増分が38                                                                                                                                                                           | AC-11。S56/TC-435 の引き継ぎ                                                                                                                                        |
| TC-485  | 日付列と作者列の境界（default case）で、日付列を40px未満にするドラッグ                                                                                                                                    | Validation - 他列の40px下限の維持                                          | `columnWidths[1]` が40で止まり、既存の下限計算が変わらない                                                                                                                                                                     | Task 3 実装内容4。S56/TC-436 の引き継ぎ                                                                                                                             |
| TC-486  | 保存済み `columnWidths` が狭い値（fixed layout）で描画し、M=82を通知                                                                                                                                      | Normal - 狭い保存幅の復元                                                  | 説明セル内幅が82以上になる `min-width` が適用され、`gitRepos[TEST_REPO].columnWidths` が保存値と `toEqual` で一致し、`saveRepoState` の送信0回                                                                                 | 自動補正で保存値を上書きしない。AC-11。S56/TC-437 の引き継ぎ                                                                                                        |
| TC-487  | TC-480の状態でwindowを縮小しresizeを発火                                                                                                                                                                  | Normal - ウィンドウ縮小                                                    | 再計測後も `min-width` が `412px` で、`saveRepoState` の送信0回                                                                                                                                                                | AC-11。S56/TC-438 の引き継ぎ                                                                                                                                        |
| TC-488  | 列境界でmousedown→mousemove→mouseupの利用者ドラッグを行い、その後に最小幅の自動補正が起きる                                                                                                               | Normal - 保存は利用者のドラッグだけ                                        | `saveRepoState` の送信がmouseup時の1回だけで、payloadの `columnWidths` がドラッグ結果と一致する。自動補正で追加送信されない                                                                                                    | AC-11。S56/TC-439 の引き継ぎ                                                                                                                                        |
| TC-489  | `columnWidths === null`（auto layout）で描画して計測                                                                                                                                                      | Normal - 表示用固定幅と保存値の分離                                        | 表のclassが `autoLayout` のまま、他4列へ計測時の自然な使用幅が表示用に固定され、`gitRepos[TEST_REPO].columnWidths` は `null` のまま、`saveRepoState` の送信0回                                                                 | §3.5。S56/TC-440 の引き継ぎ                                                                                                                                         |
| TC-490  | TC-489の後、window resizeを外寸が同じ場合と変わった場合の各分岐で発火                                                                                                                                     | Normal - 既存resize経路からの再計測                                        | どちらの分岐でも `requestAnimationFrame` が予約され、再計測の前に表示用固定幅が解除される                                                                                                                                      | 分岐より前に予約（Task 3 実装内容5）。S56/TC-441 の引き継ぎ                                                                                                         |
| TC-491  | 列ドラッグ中のmousemoveとmouseup                                                                                                                                                                          | Normal - ドラッグ中/終了の再計測                                           | 各イベントで再計測が予約され、同一フレーム内は `requestAnimationFrame` 1回にまとめられる                                                                                                                                       | AC-11。S56/TC-442 の引き継ぎ                                                                                                                                        |
| TC-492  | 実FindWidgetを読み込み、隠れたbranch名だけに一致する語で検索                                                                                                                                              | Normal - 検索通知の配線                                                    | 検索結果が1コミット、counterが `.refOverflowMatch` を持ち、`.refOverflowPopup` は0個のまま                                                                                                                                     | `onHighlightsChanged` から `syncSearchHighlights` への接続。AC-08。S56/TC-443 の引き継ぎ                                                                            |
| TC-493  | `loadCommits` で表を3回再描画した後、window resizeを1回発火                                                                                                                                               | Boundary - controllerの単一生成                                            | `requestAnimationFrame` の呼出しが1回                                                                                                                                                                                          | リスナー重複なし。AC-17。S56/TC-444 の引き継ぎ                                                                                                                      |

### 失敗源インベントリ（include-or-justify）— Feature 059-02 追加分（S60）

| 失敗源                                                   | 対応ケースまたは除外理由                                                                    |
| -------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| 実描画へ折り畳みが接続されない                           | TC-475                                                                                      |
| 計測用複製が操作対象になる                               | TC-476                                                                                      |
| 行内dblclick・バッジなし行の退行                         | TC-477、TC-478                                                                              |
| 全件収容行で最小幅を解除する（表示状態に依存した最小幅） | TC-479                                                                                      |
| 最小幅の未適用・64とnullの混同・計測不能時の解除         | TC-480〜TC-482                                                                              |
| ドラッグで説明セルがMを下回る・他列下限の変更            | TC-483〜TC-485                                                                              |
| 自動補正による保存値の上書き                             | TC-486〜TC-489                                                                              |
| 再計測契機の欠落                                         | TC-490、TC-491                                                                              |
| 検索通知の未配線                                         | TC-492                                                                                      |
| controllerの多重生成                                     | TC-493                                                                                      |
| 外部依存の失敗                                           | excluded(ホストとの通信は `saveRepoState` の送信有無だけを検証し、送信処理自体は既存の責務) |
| 例外送出                                                 | excluded(描画・列幅処理にthrow経路を追加しない)                                             |

**失敗カテゴリ網羅（diversity floor）**:

- Validation: TC-476、TC-482、TC-485
- Exception: excluded(throw経路なし)
- External: excluded(上表のとおり)
- Boundary: TC-478、TC-479、TC-481、TC-483、TC-484、TC-493
- Type: excluded(DOM型はTypeScriptで保証される)

**失敗系/正常系比（煙感知器）**: 正常系10件、失敗系9件（S56の正常系10件・失敗系8件から、TC-430 を Boundary の TC-478・TC-479 へ分割して1件増）。

### Feature 059-02 テスト対応と実行証跡（S60）

- テストファイル: `tests/web/main.refOverflow.test.ts` の describe `ref overflow rendering and the description column width (S60)` と子 describe `fixed layout column resizing`。`tests/web/main.test.ts` は FindWidget・contextMenu をファイル全体でモックしているため、実 FindWidget・実 contextMenu / dialogs（呼出し記録用のspyで実装を実行）を使う統合テストを既存の分割名規則（`tests/src/repoManager.*.test.ts` 等）に倣って別ファイルにした。本ファイルは `web/refOverflow.ts` をimportしない
- Case対応: TC-475〜TC-493 は各 `it` 名の末尾と `// Case:` に記載（TC-481 は `min-width` の 64/null と、ドラッグ下限の null 経路を fixed layout 側の `it` で分けて検証。TC-490 は外寸同一/変更の2分岐を `it.each`）。S56からの付替えはCase IDだけで、テストのロジックと期待値は変えていない（旧→新: TC-427→TC-475、TC-428→TC-476、TC-429→TC-477、TC-430→TC-478（バッジなし表）・TC-479（全件収容行）、TC-431〜TC-444→TC-480〜TC-493）
- TC-475 主回帰の RED/GREEN（S56/TC-427 として取得）: 修正前 `b11c2ed` の一時 worktree（node_modules は symlink）に同じテストファイルを置き `vitest run -t TC-427` を実行すると、`expected [] to deeply equal [ '+4' ]`（counter不在）で失敗した。同時に描画されたrefは main と長い worktree 5件の計6件で、import・fixture は正常。修正後の作業ツリーでは pass（先頭2個、`+4`、一覧4件の元順、右クリックで `buildRefContextMenuItems(TEST_REPO, 4件目の名前, 複製, false, "main", undefined, { path, isMainWorktree: false })`）
- TC-478 / TC-479 の検証: TC-478 はバッジなし行だけの表で、counter 0個・説明セルのmarkup不変・`#content` と表の `min-width` 空文字を確認する。TC-479 は全件収容行を含む表で、counter 0個・隠れref 0件・表示が元順のままであることと、表の `min-width` が数値（`405px`）であることを確認する
- 実ブラウザ確認（Chromium headless、手順は `web/refOverflow-test.md` の実行証跡と同じ環境）: viewport 1000×500 で変更前後の行高 `[24.5, 24, 24, …]`、行top、グラフ頂点 cx/cy、グラフと表の top が一致。devicePixelRatio 2、CSS zoom 1.25（行高 30.5/30）、viewport 600 でも前後一致。列ドラッグ（実マウス）後の説明セル内幅 81 = M（81）、保存は mouseup の1回だけ。保存幅 `[400,100,100,100]`・幅700 の再表示で内幅 81、表 scrollWidth 877 > clientWidth 685 で横スクロール、保存送信0回。スクリーンショットはリポジトリ外（scratchpad `t6/shots/step1-*.png`、`step2-after-drag.png`、`step2-narrow-saved-700.png`）
