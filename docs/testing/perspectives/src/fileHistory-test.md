# テスト観点表: src/fileHistory.ts

> Source: `src/fileHistory.ts`
> Generated: 2026-09-08T00:00:00Z
> Language: TypeScript
> Test Framework: Vitest
> Storage Mode: single-file

## S1: normalizeFileHistoryPath() path 正規化と 5 条件の拒否

> Origin: Feature 055-07 (light-spec-plan)
> Added: 2026-09-08
> Status: active
> Supersedes: -
> Signature: `normalizeFileHistoryPath(filePath: string): string | null`
> Target Path: `src/fileHistory.ts`（`normalizeFileHistoryPath()`。実装後に行範囲へ更新）
> Test File: `tests/src/fileHistory.test.ts`

`\` を `/` へ置換した後、空文字列・`/` 始まり・drive letter・`.` / `..` component・NUL / `U+001E` を `null` で拒否する純関数の観点（対応プラン §3.6）。Git process の起動有無は `src/dataSource-test/07-file-history-01.md` S49 の責務で本表には含めない。

| Case ID | Input / Precondition  | Perspective (Normal / Validation / Exception / External / Boundary / Type) | Expected Result                                                   | Notes                                    |
| ------- | --------------------- | -------------------------------------------------------------------------- | ----------------------------------------------------------------- | ---------------------------------------- |
| TC-001  | `"src\\a.txt"`        | Normal - backslash 正規化                                                  | 戻り値が `"src/a.txt"` と `toBe` で一致する                       | 置換後に 5 条件を判定                    |
| TC-002  | `"-weird name *.txt"` | Normal - hyphen / space / glob は拒否しない                                | 戻り値が `"-weird name *.txt"` と `toBe` で一致する（入力と同値） | `--` の後ろへ 1 要素で渡すため拒否対象外 |
| TC-003  | `""`                  | Boundary - empty                                                           | 戻り値が `null`（`toBeNull`）                                     | -                                        |
| TC-004  | `"/etc/x"`            | Validation - absolute path                                                 | 戻り値が `null`                                                   | `/` 始まり                               |
| TC-005  | `"C:/x"`              | Validation - drive letter                                                  | 戻り値が `null`                                                   | `/^[A-Za-z]:/`                           |
| TC-006  | `"a/../b"`            | Validation - parent component                                              | 戻り値が `null`                                                   | `..` component                           |
| TC-007  | `"./a"`               | Validation - dot component                                                 | 戻り値が `null`                                                   | `.` component                            |
| TC-008  | `"a\u0000b"`          | Validation - NUL 混入                                                      | 戻り値が `null`                                                   | `-z` 出力の区切りと衝突                  |
| TC-009  | `"a\u001eb"`          | Validation - record separator 混入                                         | 戻り値が `null`                                                   | `%x1e` 区切りと衝突                      |

## S2: buildLineageQueryArgs() / buildMergeQueryArgs() / buildAncestryQueryArgs() の args 構築

> Origin: Feature 055-07 (light-spec-plan)
> Added: 2026-09-08
> Status: active
> Supersedes: -
> Signature: `buildLineageQueryArgs(anchorHash: string, requestedPath: string): string[]` / `buildMergeQueryArgs(anchorHash: string, boundaryHash: string | null, historicalPaths: readonly string[]): string[]` / `buildAncestryQueryArgs(anchorHash: string, boundaryParentHashes: readonly string[]): string[]`
> Target Path: `src/fileHistory.ts`（3 つの args 構築関数。実装後に行範囲へ更新）
> Test File: `tests/src/fileHistory.test.ts`

対応プラン §3.5 / §4 Task 2 実装内容 3 の配列を `toEqual` で完全一致させる観点。path は `--` の後ろに 1 要素ずつ置き、quote や escape をしない。

| Case ID | Input / Precondition                                                                   | Perspective (Normal / Validation / Exception / External / Boundary / Type) | Expected Result                                                                                                                                                                                                                                                            | Notes                             |
| ------- | -------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- |
| TC-010  | `buildLineageQueryArgs("abc123", "src/a.txt")`                                         | Normal - lineage args 完全一致                                             | 戻り値が `["--literal-pathspecs", "log", "--topo-order", "--follow", "--format=%x1e%H%x00%P%x00", "--name-status", "--find-renames", "--diff-filter=AMDR", "-z", "abc123", "--", "src/a.txt"]` と `toEqual` で一致する                                                     | 採用引数は確定仕様 10 章の集合内  |
| TC-011  | `buildLineageQueryArgs("abc123", "-weird name *.txt")`                                 | Boundary - hyphen / space / glob を含む path                               | 末尾 3 要素が `["abc123", "--", "-weird name *.txt"]` と `toEqual` で一致し、path が 1 要素で quote されていない                                                                                                                                                           | shell を経由しない前提の固定      |
| TC-012  | `buildMergeQueryArgs("abc123", null, ["a", "b"])`                                      | Normal - boundary なし merge args                                          | 戻り値が `["--literal-pathspecs", "log", "--topo-order", "--merges", "--diff-merges=separate", "--format=%x1e%H%x00%P%x00", "--name-status", "--find-renames", "--diff-filter=AMDR", "-z", "abc123", "--", "a", "b"]` と `toEqual` で一致し、`^` で始まる要素が 0 件である | -                                 |
| TC-013  | `buildMergeQueryArgs("abc123", "def456", ["a"])`                                       | Normal - boundary 付き merge args                                          | `"abc123"` の次の要素が `"^def456"`、その次が `"--"`、末尾が `"a"` である                                                                                                                                                                                                  | `^` は boundary hash の先頭へ連結 |
| TC-014  | `buildMergeQueryArgs("abc123", null, ["src/current-name.txt", "src/legacy-name.txt"])` | Normal - historical path の順序維持                                        | `"--"` 以降が `["src/current-name.txt", "src/legacy-name.txt"]` と `toEqual` で一致する（requested path → rename 元の挿入順）                                                                                                                                              | `Array.from(set)` の順序          |
| TC-015  | `buildAncestryQueryArgs("abc123", [])`                                                 | Boundary - boundary 親 0 件                                                | 戻り値が `["rev-list", "--parents", "abc123"]` と `toEqual` で一致する                                                                                                                                                                                                     | boundary なし / root boundary     |
| TC-016  | `buildAncestryQueryArgs("abc123", ["p1", "p2"])`                                       | Normal - 親ごとの `^`                                                      | 戻り値が `["rev-list", "--parents", "abc123", "^p1", "^p2"]` と `toEqual` で一致する                                                                                                                                                                                       | 親 hash ごとに 1 つ               |
| TC-017  | `Object.freeze(["a", "b"])` を `buildMergeQueryArgs()` の第 3 引数に渡す               | Normal - 入力非破壊                                                        | 例外を送出せず、戻り値が入力配列と別参照（`not.toBe`）で、入力配列が `["a", "b"]` のまま                                                                                                                                                                                   | readonly 引数の保証               |

## S3: parseFileHistoryRecords() record parse と group 集約

> Origin: Feature 055-07 (light-spec-plan)
> Added: 2026-09-08
> Status: active
> Supersedes: -
> Signature: `parseFileHistoryRecords(stdout: string): FileHistoryRecordGroup[] | null`
> Target Path: `src/fileHistory.ts`（`parseFileHistoryRecords()`。実装後に行範囲へ更新）
> Test File: `tests/src/fileHistory.test.ts`

確定仕様 3.2.3 の byte layout（`\x1e<hash>\x00<parents>\x00\x00\n<status>\x00<path>\x00`）を parse し、malformed は部分結果を返さず `null` にする fail-closed の観点（対応プラン §3.6）。hash の有効判定は既存 `isValidCommitHash()`（4〜40 桁 hex）に委ねる。

| Case ID | Input / Precondition                                                                                           | Perspective (Normal / Validation / Exception / External / Boundary / Type) | Expected Result                                                                                                                                                                                          | Notes                                   |
| ------- | -------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------- |
| TC-018  | `""`                                                                                                           | Boundary - empty stdout                                                    | 戻り値が `[]` と `toEqual` で一致する（`null` ではない）                                                                                                                                                 | record 0 件は成功                       |
| TC-019  | §3.5 の lineage 1 record `\x1e<h>\x00<p>\x00\x00\nM\x00src/current-name.txt\x00`                               | Normal - M record                                                          | 戻り値が group 1 件で、`hash` が `<h>`、`parentHashes` が `[<p>]`、`records[0].entries` が `[{ status: "M", oldPath: "src/current-name.txt", newPath: "src/current-name.txt" }]` と `toEqual` で一致する | `A` / `M` / `D` は old = new で同じ分岐 |
| TC-020  | §3.5 の merge 1 record `\x1e<h>\x00<p1> <p2>\x00\x00\nR077\x00src/legacy-name.txt\x00src/current-name.txt\x00` | Normal - R record                                                          | `parentHashes` が `[<p1>, <p2>]`、entries が `[{ status: "R", oldPath: "src/legacy-name.txt", newPath: "src/current-name.txt" }]` と一致する                                                             | score 3 桁は捨てる                      |
| TC-021  | 親 field が空文字列の record（root commit）                                                                    | Boundary - 親 0 件                                                         | `parentHashes` が `[]` と `toEqual` で一致する                                                                                                                                                           | root は `%P` が空                       |
| TC-022  | 同じ hash の record が連続 2 件（各 record に 2 entry）、その後に別 hash の record 1 件                        | Normal - group 集約と出力順維持                                            | 戻り値が group 2 件で、先頭 group の `records.length` が 2、各 record の `entries` が Git 出力順に 2 件ずつ保持され、group の `hash` の並びが入力順と一致する                                            | 同 hash は 1 group。並べ替えしない      |
| TC-023  | `"x"` で始まる stdout                                                                                          | Type - 先頭 separator なし                                                 | 戻り値が `null`                                                                                                                                                                                          | `stdout[0] !== "\x1e"`                  |
| TC-024  | `fields[2]` が空文字列でない record                                                                            | Type - layout 不一致                                                       | 戻り値が `null`                                                                                                                                                                                          | `%P` の後ろの空 field                   |
| TC-025  | 末尾要素が空文字列でない record（末尾 NUL 欠落）                                                               | Type - 末尾 terminator 欠落                                                | 戻り値が `null`                                                                                                                                                                                          | -                                       |
| TC-026  | `\x1e<h>\x00<p>\x00\x00` のみ（entries なし record）                                                           | Validation - entries なし record                                           | 戻り値が `null`                                                                                                                                                                                          | `combined` でのみ現れる形を拒否         |
| TC-027  | entries 列の先頭要素が `\n` で始まらない record                                                                | Type - status 前の LF 欠落                                                 | 戻り値が `null`                                                                                                                                                                                          | -                                       |
| TC-028  | status `T` / `C100` / `AM` のいずれか                                                                          | Validation - 未知 status                                                   | 3 入力とも戻り値が `null`                                                                                                                                                                                | `--diff-filter=AMDR` 外。同じ分岐       |
| TC-029  | status `R99`（数字 2 桁）                                                                                      | Type - R score 桁数不正                                                    | 戻り値が `null`                                                                                                                                                                                          | `R` + 3 桁のみ受理                      |
| TC-030  | status `R999`                                                                                                  | Boundary - R score 最大値                                                  | 戻り値が group 1 件で entries の `status` が `"R"`（拒否しない）                                                                                                                                         | 確定仕様 10.1「対応不要」               |
| TC-031  | `M` の後ろに path field がない record                                                                          | Validation - path 欠損                                                     | 戻り値が `null`                                                                                                                                                                                          | -                                       |
| TC-032  | `M\x00\x00` のように path が空文字列                                                                           | Boundary - path 空文字列                                                   | 戻り値が `null`                                                                                                                                                                                          | 欠損と空を同一に拒否                    |
| TC-033  | `R100\x00old.txt\x00` のみ（newPath 欠損）                                                                     | Validation - R の第 2 path 欠損                                            | 戻り値が `null`                                                                                                                                                                                          | -                                       |
| TC-034  | hash が `zz`（`isValidCommitHash()` 偽）                                                                       | Type - 不正 hash                                                           | 戻り値が `null`                                                                                                                                                                                          | -                                       |
| TC-035  | 親 hash 列に `g1` を含む record                                                                                | Type - 不正 親 hash                                                        | 戻り値が `null`                                                                                                                                                                                          | 親も同じ検証                            |
| TC-036  | 先頭 record は正常、2 件目が malformed                                                                         | Validation - 部分結果の禁止                                                | 戻り値が `null` で、正常 record だけの配列を返さない                                                                                                                                                     | fail-closed                             |

## S4: applyLineageRules() rename 遷移と birth boundary

> Origin: Feature 055-07 (light-spec-plan)
> Added: 2026-09-08
> Status: active
> Supersedes: -
> Signature: `applyLineageRules(groups: readonly FileHistoryRecordGroup[], anchorHash: string, requestedPath: string): LineageResult | null`
> Target Path: `src/fileHistory.ts`（`applyLineageRules()`。実装後に行範囲へ更新）
> Test File: `tests/src/fileHistory.test.ts`

lineage query の group を Git 出力順に走査し、tracked path を rename で切り替え、anchor 以外の `D` は含めず停止、非 merge `A` は含めて停止する規則の観点（対応プラン §3.6）。

| Case ID | Input / Precondition                                                                                                                                                                                        | Perspective (Normal / Validation / Exception / External / Boundary / Type) | Expected Result                                                                                                                                                                                               | Notes                                   |
| ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------- |
| TC-037  | `similar` 系の lineage 5 group（fix `M current` / update renamed `M current` / rename `R legacy→current` / update legacy `M legacy` / base `A legacy`）、anchor = fix、requestedPath `src/current-name.txt` | Normal - rename 遷移の end-to-end                                          | `entries` が 5 件で type が `["M", "M", "R", "M", "A"]`、`historicalPaths` が `["src/current-name.txt", "src/legacy-name.txt"]`、`boundary` が base group の `{ hash, parentHashes }` と `toEqual` で一致する | 確定仕様 8.1 の lineage 側              |
| TC-038  | TC-037 の rename entry                                                                                                                                                                                      | Normal - R entry の path 3 値                                              | 当該 entry が `oldFilePath: "src/legacy-name.txt"`、`newFilePath: "src/current-name.txt"`、`historicalPath: "src/current-name.txt"` である                                                                    | `R` は new = historical                 |
| TC-039  | TC-037 の update legacy entry                                                                                                                                                                               | Normal - rename 後の tracked path                                          | 当該 entry が `oldFilePath === newFilePath === historicalPath === "src/legacy-name.txt"` で、rename より後ろの group が `legacy` で選択されている                                                             | tracked path 切替の観測                 |
| TC-040  | group の先頭 record に `M current` と `R legacy→current` の両 entry                                                                                                                                         | Normal - R 最優先                                                          | 選ばれる entry の type が `"R"` で `historicalPaths` に `legacy` が追加される                                                                                                                                 | R 以外が複数なら出力順先頭              |
| TC-041  | 先頭 record に tracked path と無関係な entry しかない group                                                                                                                                                 | Validation - 該当 entry なし                                               | 戻り値が `null`                                                                                                                                                                                               | 無関係 record だけの group              |
| TC-042  | 先頭 record に該当なし、2 件目の record に該当 entry がある group                                                                                                                                           | Validation - 先頭 record 限定                                              | 戻り値が `null`（2 件目の record は参照しない）                                                                                                                                                               | lineage query は 1 record / commit      |
| TC-043  | anchor 以外の group が `D`                                                                                                                                                                                  | Normal - D 停止                                                            | `entries` に当該 group を含まず、`boundary` が当該 group の `{ hash, parentHashes }`、以降の group は無視される                                                                                               | birth boundary は `D` を含まない        |
| TC-044  | anchor 自身（`group.hash === anchorHash`）が `D`                                                                                                                                                            | Boundary - anchor 自身の D                                                 | 当該 group が type `"D"` の entry として含まれ、走査が続く                                                                                                                                                    | `similar` の削除 anchor（7 件）         |
| TC-045  | 親 1 件の group が `A`                                                                                                                                                                                      | Normal - 非 merge A 停止                                                   | 当該 group が type `"A"` / `isMerge: false` で含まれ、`boundary` が当該 group、以降の group は無視される                                                                                                      | birth boundary は `A` を含む            |
| TC-046  | 親 2 件の group が `A`                                                                                                                                                                                      | Boundary - merge A は停止しない                                            | 当該 group が type `"A"` / `isMerge: true` で含まれ、次の group の走査が続く                                                                                                                                  | `isMerge` は `parentHashes.length >= 2` |
| TC-047  | 末尾まで `A` / `D` 停止が起きない group 列                                                                                                                                                                  | Boundary - 末尾到達                                                        | `boundary` が `null` で全 group が entries に含まれる                                                                                                                                                         | CE6 の lineage                          |
| TC-048  | `groups` が `[]`                                                                                                                                                                                            | Boundary - 0 group                                                         | `{ entries: [], historicalPaths: [requestedPath], boundary: null }` と `toEqual` で一致する                                                                                                                   | lineage 0 件（F1）                      |
| TC-049  | `Object.freeze` した groups を渡す                                                                                                                                                                          | Normal - 入力非破壊                                                        | 例外を送出せず、入力 groups の内容が呼出前と `toEqual` で一致する                                                                                                                                             | -                                       |

## S5: applyMergeRules() merge entry と誕生 merge 候補

> Origin: Feature 055-07 (light-spec-plan)
> Added: 2026-09-08
> Status: active
> Supersedes: -
> Signature: `applyMergeRules(groups: readonly FileHistoryRecordGroup[], historicalPaths: readonly string[], lineageHashes: ReadonlySet<string>): MergeResult | null`
> Target Path: `src/fileHistory.ts`（`applyMergeRules()`。実装後に行範囲へ更新）
> Test File: `tests/src/fileHistory.test.ts`

`--diff-merges=separate` の group から親ごとの record を 1 entry に畳み、誕生 merge 候補（record 数 = 親数かつ全 record `A`）を判定し、lineage と重複する hash を捨てる規則の観点（対応プラン §3.6）。

| Case ID | Input / Precondition                                                                                         | Perspective (Normal / Validation / Exception / External / Boundary / Type) | Expected Result                                                                                                                                                                                            | Notes                           |
| ------- | ------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------- |
| TC-050  | 親 2 件・record 1 件（`R077 legacy→current`）の group、historicalPaths `[current, legacy]`、lineageHashes 空 | Normal - R merge entry                                                     | `entries` が 1 件で `{ type: "R", oldFilePath: "src/legacy-name.txt", newFilePath: "src/current-name.txt", historicalPath: "src/current-name.txt", isMerge: true }` を含み、`birthCandidateHashes` が `[]` | `similar` の merge              |
| TC-051  | 親 2 件・record 2 件（それぞれ `M current`）の group                                                         | Normal - 複数 record を 1 entry                                            | `entries.length` が 1 で、type が選択ありの先頭 record の status `"M"` である                                                                                                                              | 確定仕様 8.1「entry は 1 件」   |
| TC-052  | 親 2 件・record 2 件がともに `A <historical path>`、候補条件を満たす group が 2 件                           | Normal - 誕生 merge 判定                                                   | 両 entry の type が `"A"` で、`birthCandidateHashes` が Git 出力順の 2 hash と `toEqual` で一致する                                                                                                        | record 数 = 親数かつ全 A        |
| TC-053  | 親 2 件・record 1 件だけが `A`                                                                               | Boundary - record 数 < 親数                                                | type は `"A"` だが `birthCandidateHashes` が `[]` である                                                                                                                                                   | 片親にしか差分がない merge      |
| TC-054  | 親 2 件・record 2 件が `A` と `M`                                                                            | Boundary - 全 A でない                                                     | type が先頭 record の選択 entry の status で、`birthCandidateHashes` が `[]`                                                                                                                               | -                               |
| TC-055  | record の entries が `[M other.txt, M current]`                                                              | Normal - 選択 entry は historical path の出力順先頭                        | 選ばれる entry の `historicalPath` が `"src/current-name.txt"` で、`other.txt` は使われない                                                                                                                | pathspec 外 entry は無視        |
| TC-056  | 全 record に historicalPaths の newPath を持つ entry がない group                                            | Validation - 選択なし                                                      | 戻り値が `null`                                                                                                                                                                                            | 無関係 record だけの group      |
| TC-057  | record 2 件のうち 1 件だけ選択 entry あり                                                                    | Normal - 選択なし record を飛ばす                                          | `entries` が 1 件で type が選択ありの record の status、`birthCandidateHashes` が `[]`（全 record A の条件を満たさない）                                                                                   | -                               |
| TC-058  | `parentHashes.length` が 1 の group を含む                                                                   | Validation - 親 1 件                                                       | 戻り値が `null`                                                                                                                                                                                            | 確定仕様 8.3。親 0 件も同じ分岐 |
| TC-059  | 候補条件を満たす group の hash が `lineageHashes` に含まれる                                                 | Normal - lineage 重複除去（entries）                                       | `entries` に当該 hash が含まれない                                                                                                                                                                         | 同 hash は lineage 優先         |
| TC-060  | TC-059 と同じ入力                                                                                            | Normal - lineage 重複除去（候補）                                          | `birthCandidateHashes` にも当該 hash が含まれない                                                                                                                                                          | 候補からも除く                  |
| TC-061  | `groups` が `[]`                                                                                             | Boundary - 0 group                                                         | `{ entries: [], birthCandidateHashes: [] }` と `toEqual` で一致する                                                                                                                                        | 空 stdout の merge query        |

## S6: parseAncestryGraph() rev-list --parents の parse

> Origin: Feature 055-07 (light-spec-plan)
> Added: 2026-09-08
> Status: active
> Supersedes: -
> Signature: `parseAncestryGraph(stdout: string): Map<string, string[]> | null`
> Target Path: `src/fileHistory.ts`（`parseAncestryGraph()`。実装後に行範囲へ更新）
> Test File: `tests/src/fileHistory.test.ts`

| Case ID | Input / Precondition                            | Perspective (Normal / Validation / Exception / External / Boundary / Type) | Expected Result                                                                            | Notes                            |
| ------- | ----------------------------------------------- | -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ | -------------------------------- |
| TC-062  | `"<h1> <p1> <p2>\n<h2> <p1>\n\n"`（末尾に空行） | Normal - 親配列と空行の無視                                                | `Map` の `size` が 2、`get(h1)` が `[p1, p2]`、`get(h2)` が `[p1]` と `toEqual` で一致する | key は commit hash。空行は捨てる |
| TC-063  | `"<h3>\n"`（root commit 行）                    | Boundary - 親 0 件                                                         | `get(h3)` が `[]` と `toEqual` で一致する                                                  | -                                |
| TC-064  | `""`                                            | Boundary - empty stdout                                                    | 戻り値が `size` 0 の `Map`（`null` ではない）                                              | -                                |
| TC-065  | token に `zz` を含む行                          | Type - 不正 hash token                                                     | 戻り値が `null`                                                                            | 確定仕様 F2「失敗」              |

## S7: resolveFileHistoryEntries() 有効候補の判定と真の祖先の除外

> Origin: Feature 055-07 (light-spec-plan)
> Added: 2026-09-08
> Status: active
> Supersedes: -
> Signature: `resolveFileHistoryEntries(input: ResolveFileHistoryInput): FileHistoryEntry[]`
> Target Path: `src/fileHistory.ts`（`resolveFileHistoryEntries()`。実装後に行範囲へ更新）
> Test File: `tests/src/fileHistory.test.ts`

lineage と merge の entry を連結し、有効候補（historical path が requested path、または rename 元 path の `R` entry すべての真の祖先）の真の祖先集合を除外する規則の観点（対応プラン §3.6、確定仕様 F2）。

| Case ID | Input / Precondition                                                                                                             | Perspective (Normal / Validation / Exception / External / Boundary / Type) | Expected Result                                                                                    | Notes                               |
| ------- | -------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | ----------------------------------- |
| TC-066  | `ancestry: null`、lineage 2 件・merge 1 件                                                                                       | Normal - ancestry 省略時の連結                                             | 戻り値が `[...lineage.entries, ...merge.entries]` と `toEqual` で一致する（順序も同じ）            | ancestry query 未実行               |
| TC-067  | 候補の `historicalPath` が `requestedPath` と等しく、ancestry で候補の真の祖先に lineage の `A` と merge の `D` がある（CE3 形） | Normal - requested path 候補の除外                                         | 戻り値が `[edit M, recreate A*]` の 2 件で、旧 `A` と delete merge の `D` を含まず、候補自身は残る | 有効候補は残す                      |
| TC-068  | 候補の `historicalPath` が rename 元 path で、lineage の `R`（`oldFilePath` = その path）の真の祖先（CE6 形）                    | Normal - rename 元候補の有効判定                                           | 戻り値が 3 件（fix M / rename R / birth A\*）で候補が含まれる                                      | (b) 条件                            |
| TC-069  | 候補の `historicalPath` が rename 元 path だが、`R` entry の真の祖先ではない（CE5 形）                                           | Validation - 無効候補は除外に使わない                                      | 戻り値が 7 件で、候補が type `"A"` の通常 merge entry として残り、他の entry も除外されない        | 旧 path の再利用                    |
| TC-070  | 候補の `historicalPath` が requested path でも lineage の `R` の `oldFilePath` でもない                                          | Validation - 該当 R 0 件                                                   | 候補は無効として残り、除外は起きない                                                               | -                                   |
| TC-071  | 有効候補 C1 の真の祖先に別の有効候補 C2 がある                                                                                   | Normal - 候補同士の祖先除外                                                | 戻り値に C1 が含まれ C2 が含まれない                                                               | CE7 形                              |
| TC-072  | 有効候補 2 件が祖先関係にない                                                                                                    | Boundary - 祖先関係なし                                                    | 戻り値に両候補が含まれる（`null` や例外にならない）                                                | 確定仕様 F2「両方を誕生として残す」 |
| TC-073  | 候補 hash が `ancestry` の `Map` に無い                                                                                          | Boundary - Map 未登録 hash                                                 | 例外を送出せず、当該候補の真の祖先集合が空として扱われ combined がそのまま返る                     | BFS を打ち切る                      |
| TC-074  | lineage 0 件・merge 0 件で `ancestry` が空 `Map`                                                                                 | Boundary - 0 件                                                            | 戻り値が `[]`                                                                                      | -                                   |

## S8: 実測 fixture 文字列の合成（parse → lineage → merge → ancestry → resolve）

> Origin: Feature 055-07 (light-spec-plan)
> Added: 2026-09-08
> Status: active
> Supersedes: -
> Signature: `parseFileHistoryRecords()` → `applyLineageRules()` → `applyMergeRules()` → `parseAncestryGraph()` → `resolveFileHistoryEntries()` の合成
> Target Path: `src/fileHistory.ts`（全 export の合成。実装後に行範囲へ更新）
> Test File: `tests/src/fileHistory.test.ts`

確定仕様「実測根拠（2026-09-08）」の lineage / merge / ancestry stdout を fixture 文字列として与え、期待 entry（subject 対応の hash、type、`isMerge`、`historicalPath`）と一致させる観点（対応プラン §4 Task 2 完了条件）。real repository での Git 実行は `src/dataSource-test/07-file-history-01.md` S50 の責務。

| Case ID | Input / Precondition                                                       | Perspective (Normal / Validation / Exception / External / Boundary / Type) | Expected Result                                                                                                                                                                                                                                                                  | Notes                                   |
| ------- | -------------------------------------------------------------------------- | -------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------- |
| TC-075  | `similar`（anchor `fix: update file after merge`、ancestry なし）          | Normal - 6 系統の典型                                                      | 6 件: fix `M`、`merge: rename feature` `R` / `isMerge: true`、`feat: update renamed file` `M`、`refactor: rename tracked file` `R`（historicalPath `src/current-name.txt`）、`feat: update legacy file` `M`（historicalPath `src/legacy-name.txt`）、`base: add legacy file` `A` | 候補 0 件のため ancestry 省略           |
| TC-076  | `evil`（anchor `edit new f`、lineage は `D` 停止 boundary、ancestry あり） | Normal - D 停止と誕生 merge 除外                                           | 2 件: `edit new f` `M`、`evil merge: creates f` `A` / `isMerge: true`。`old incarnation` の hash を含まない                                                                                                                                                                      | boundary は `old incarnation: delete f` |
| TC-077  | CE3（anchor `edit new f`）                                                 | Normal - merge だけの削除・再作成                                          | 2 件: `edit new f` `M`、`recreate merge: creates f` `A` / `isMerge: true`。`old incarnation: add f` と `delete merge: removes f` を含まない                                                                                                                                      | 除外あり                                |
| TC-078  | CE5（anchor `fix: update current after reuse`）                            | Validation - 無効候補で除外なし                                            | 7 件で `reuse merge: creates legacy path` が `A` / `isMerge: true` として残り、base `A` も残る                                                                                                                                                                                   | 候補は無効                              |
| TC-079  | CE6（anchor `fix: edit current`、boundary `null`）                         | Normal - 有効候補が rename 元                                              | 3 件: fix `M`、`refactor: rename tracked file` `R`、`birth merge: creates legacy` `A` / `isMerge: true`                                                                                                                                                                          | ancestry の `^` 引数なし                |
| TC-080  | CE7（anchor `recreate merge: creates f`）                                  | Boundary - anchor 自身が有効候補                                           | 1 件: `recreate merge: creates f` `A` / `isMerge: true`。`old incarnation: add f` / `old incarnation: edit f` / `delete merge: removes f` を含まない                                                                                                                             | 候補自身は残る                          |

### 失敗源インベントリ（include-or-justify）— Feature 055-07 追加分（S1〜S8）

| 失敗源                                                                      | 対応ケースまたは除外理由                                                                                                                                                                                  |
| --------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 不正 path の受理（absolute / drive / `.` `..` / NUL / `U+001E` / empty）    | TC-003〜TC-009                                                                                                                                                                                            |
| path の quote・escape・分割による pathspec 誤解釈                           | TC-011、TC-014                                                                                                                                                                                            |
| `^` 引数の欠落・過剰（boundary なし / あり、親ごと）                        | TC-012、TC-013、TC-015、TC-016                                                                                                                                                                            |
| malformed stdout での部分結果返却                                           | TC-023〜TC-029、TC-031〜TC-036                                                                                                                                                                            |
| entries を持たない record・未知 status の受理                               | TC-026、TC-028                                                                                                                                                                                            |
| 不正 hash / 親 hash / token の受理                                          | TC-034、TC-035、TC-065                                                                                                                                                                                    |
| rename 遷移の取り違え（tracked path 切替漏れ、R 優先漏れ）                  | TC-037〜TC-040                                                                                                                                                                                            |
| birth boundary の誤判定（`D` を含める / `A` を含めない / merge `A` で停止） | TC-043〜TC-047                                                                                                                                                                                            |
| 無関係 record だけの group での継続                                         | TC-041、TC-042、TC-056                                                                                                                                                                                    |
| 親 1 件 record を merge として扱う                                          | TC-058                                                                                                                                                                                                    |
| 誕生 merge 候補の誤判定（record 数 / 全 A）                                 | TC-052〜TC-054、TC-057                                                                                                                                                                                    |
| lineage と merge の同 hash 重複                                             | TC-059、TC-060                                                                                                                                                                                            |
| 無効候補による誤除外・有効候補の取り漏れ                                    | TC-067〜TC-072、TC-076〜TC-080                                                                                                                                                                            |
| ancestry `Map` 未登録 hash での例外                                         | TC-073                                                                                                                                                                                                    |
| 入力配列の破壊                                                              | TC-017、TC-049                                                                                                                                                                                            |
| 境界値（0 / minimum / maximum / +/-1 / empty / NULL）                       | 0 group: TC-048、TC-061。empty stdout: TC-018、TC-064。親 0 件: TC-021、TC-063。R score 最大: TC-030。0 件合成: TC-074。maximum / +/-1: excluded(数値閾値は R score 3 桁のみで、桁数不正は TC-029 が充足) |
| 外部依存×失敗モード                                                         | excluded(純関数で `child_process` / `vscode` に依存しない。Git 失敗は `src/dataSource-test/07-file-history-01.md` S49 の責務)                                                                             |
| 例外・エラー経路                                                            | excluded(本 module は例外を投げず `null` を返す設計。`null` の分岐は上表の Validation / Type ケースで固定)                                                                                                |

**失敗カテゴリ網羅（diversity floor）**:

- Validation: TC-004〜TC-009、TC-026、TC-028、TC-031、TC-033、TC-036、TC-041、TC-042、TC-056、TC-058、TC-069、TC-070、TC-078
- Exception: excluded(throw 経路なし。上表のとおり)
- External: excluded(外部依存なし。上表のとおり)
- Boundary: TC-003、TC-011、TC-015、TC-018、TC-021、TC-030、TC-032、TC-044、TC-046〜TC-048、TC-053、TC-054、TC-061、TC-063、TC-064、TC-072〜TC-074、TC-080
- Type: TC-023〜TC-025、TC-027、TC-029、TC-034、TC-035、TC-065
- Normal: TC-001、TC-002、TC-010、TC-012〜TC-014、TC-016、TC-017、TC-019、TC-020、TC-022、TC-037〜TC-040、TC-043、TC-045、TC-049〜TC-052、TC-055、TC-057、TC-059、TC-060、TC-062、TC-066〜TC-068、TC-071、TC-075〜TC-077、TC-079

**失敗系/正常系比（煙感知器）**: 正常系35件、失敗系45件。parser と規則の正常分岐（layout・rename 遷移・merge 畳み込み・fixture 合成）が多いため比は 1.3 倍に留まるが、失敗源は上表で列挙済みで、インベントリ欠落ではないことを再導出で確認した。比率合わせのためのケース追加・削除は行わない。
