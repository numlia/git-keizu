# テスト観点表: src/dataSource.ts

> Source: `src/dataSource.ts`
> Generated: 2026-08-08T00:00:00Z
> Language: TypeScript
> Test Framework: Vitest
> Responsibility: branch-worktree

## S47: getWorktrees() collection 化と pinned detached commit の取得・統合

> Origin: Feature 052 (detached-worktree-display) (light-spec-plan)
> Added: 2026-08-08
> Status: active
> Supersedes: S22
> Signature: `getWorktrees(repo: string): Promise<WorktreeCollection>` / `getCommits(repo, branches, maxCommits, showRemoteBranches, authors, commitOrdering)`
> Target Path: `src/dataSource.ts`（`getWorktrees()` と `getCommits()` の pinned query / 統合。実装後に行範囲へ更新）
> Test File: `tests/src/dataSource.test.ts`

`getWorktrees()` の戻り値と失敗時 fallback を `WorktreeMap` / `{}` から `WorktreeCollection` / `{ branches: {}, detached: [] }` へ変え、`getCommits()` で worktree 取得を先行させたうえで、通常表示窓の外にある detached HEAD を `git log --no-walk=sorted` の別 query（pinned query）から取得して通常コミット列の末尾へ統合する変更。旧 S22 は戻り値が flat map、失敗時 fallback が `{}` であることを固定していたため supersede する。porcelain の分類規則は `src/worktree-test.md` S2、hash 値域は `src/utils-test.md` S7、ラベル描画は `web/main-test/01-rendering-02.md` S48 の責務。通常 log の `--max-count` / order flag / branch 引数の構成そのものは既存 S21 の責務で変更しない。

| Case ID | Input / Precondition                                                                                         | Perspective (Normal / Validation / Exception / External / Boundary / Type) | Expected Result                                                                                                                                                                                         | Notes                                       |
| ------- | ------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- |
| TC-292  | `getWorktrees()`、`git worktree list --porcelain` が exit 0 で branch 1 件・detached 1 件の porcelain を返す | Normal - collection passthrough                                            | `parseWorktreeList()` の戻り値がそのまま返り、`branches` と `detached` の双方が `toEqual` で一致する                                                                                                    | S22/TC-132 の引き継ぎ                       |
| TC-293  | `getWorktrees()`、`git worktree list --porcelain` が exit 非 0                                               | Exception - collection fallback                                            | 戻り値が `{ branches: {}, detached: [] }` と `toEqual` で一致する（`{}` ではない）                                                                                                                      | S22/TC-133 の置き換え                       |
| TC-294  | `getCommits()`、detached 1 件を返す worktree query と通常 log を実行                                         | Normal - worktree query precedes log                                       | `cp.spawn` の呼び出し順で `["worktree", "list", "--porcelain"]` が `--no-walk=sorted` を含む args より前に完了する                                                                                      | 4.3 の取得順（安全な引数確定）              |
| TC-295  | `getCommits()`、worktree collection の `detached` が `[]`                                                    | Boundary - no detached head                                                | `--no-walk=sorted` を含む args での `cp.spawn` の call count が 0 である                                                                                                                                | pinned query を実行しない                   |
| TC-296  | `detached` が同じ `head` を持つ 2 entry（path が異なる）                                                     | Boundary - duplicate head deduplicated                                     | pinned args の revision 部分が当該 hash 1 個だけである（2 個並ばない）                                                                                                                                  | 重複排除                                    |
| TC-297  | `detached` が `head` の異なる 2 entry、authors 未指定                                                        | Normal - pinned query args                                                 | pinned の `cp.spawn` が `["log", "--no-walk=sorted", "--format=<gitLogFormat>", head1, head2]` で 1 回呼ばれ、args に `--max-count` / `--date-order` / `--branches` / `--tags` / `--remotes` を含まない | 4.3 の pinned query 契約                    |
| TC-298  | `authors = ["Alice"]`、detached 1 件                                                                         | Normal - author filter applied to pinned                                   | pinned args に `--author=Alice` が含まれ、通常 log へ渡された `--author` の値と一致する                                                                                                                 | 4.3 の author 適用                          |
| TC-299  | `authors = ["a.b+c"]`（正規表現メタ文字を含む）                                                              | Boundary - author regex escaped                                            | pinned args の `--author` の値が `escapeRegExp()` 適用後の文字列で、通常 log と同じ値である                                                                                                             | escape 規則の共有                           |
| TC-300  | `branches = ["feature/x"]`、detached 1 件                                                                    | Validation - branch filter not applied to pinned                           | pinned args に `"feature/x"` / `--branches` / `--tags` / `--remotes` を含まない                                                                                                                         | 4.3 の branch 非適用                        |
| TC-301  | `detached` に collection 経由で `"zzzz"`（不正 hash）が混入した状態                                          | Validation - unvalidated head rejected                                     | 不正 hash が pinned args に含まれない。有効 head が他にない場合は pinned の `cp.spawn` の call count が 0 である                                                                                        | Git 引数へ検証済み head だけを渡す          |
| TC-302  | pinned commit の hash が通常 log の結果に存在しない                                                          | Normal - out-of-window append                                              | 返却 `commits` の末尾が pinned commit で、その hash の出現回数が 1 である                                                                                                                               | 8.1 の解消ケース                            |
| TC-303  | pinned commit の hash が通常 log の結果に既に存在する                                                        | Boundary - duplicate hash skipped                                          | 返却 `commits` の件数が通常 log 由来の件数と一致し、その hash の出現回数が 1 である                                                                                                                     | 8.3 の重複排除ガード                        |
| TC-304  | 同じ `head` を指す detached linked worktree 2 件、その hash は通常窓外                                       | Boundary - one row for multiple worktrees                                  | 返却 `commits` 内の当該 hash の出現回数が 1 で、返却 `worktrees.detached` の長さが 2 である                                                                                                             | 8.1 の 1 行・複数ラベル                     |
| TC-305  | 通常 log が `maxCommits + 1` 件を返し、通常窓外の pinned commit が 1 件ある                                  | Boundary - more flag excludes pinned                                       | `moreCommitsAvailable` が `true`、返却 `commits` の件数が `maxCommits + 1`（通常 `maxCommits` 件 + pinned 1 件）である                                                                                  | pinned を件数判定に含めない                 |
| TC-306  | 通常 log が `maxCommits` 件以下を返し、通常窓外の pinned commit が 1 件ある                                  | Boundary - more flag false with pinned                                     | `moreCommitsAvailable` が `false` である                                                                                                                                                                | pinned で `true` にしない                   |
| TC-307  | 通常 log が hash A, B, C の順で返り、pinned commit D を統合する                                              | Normal - normal order preserved                                            | 返却 `commits` の hash 列が `[A, B, C, D]` である（通常列内部の並びが変わらない）                                                                                                                       | 4.3 の順序契約                              |
| TC-308  | pinned query が exit 非 0 で stderr を出す                                                                   | Exception - pinned failure fallback                                        | 通常 log 由来の `commits` がそのまま返り、reject されない。返却 `worktrees` は取得済み collection のままである                                                                                          | 4.3 の局所化                                |
| TC-309  | worktree query が exit 非 0（pinned の入力が空 fallback になる）                                             | External - worktree query failure                                          | pinned の `cp.spawn` の call count が 0 で、通常 log 由来の `commits` が返り、返却 `worktrees` が `{ branches: {}, detached: [] }` である                                                               | 失敗を「worktree なし」として扱う既存契約   |
| TC-310  | uncommitted changes あり、stash あり、pinned commit が ref を持つ状態で `getCommits()` を実行                | Normal - merge before post-processing                                      | 返却 `commits` の先頭が uncommitted changes 行、stash 行が既存位置に挿入され、末尾の pinned commit に `refs` が付与されている                                                                           | 統合を node 化・stash・ref 付与より前に行う |
| TC-311  | `detached` に `isMain: true` の entry（detached main worktree）が 1 件だけある                               | Normal - detached main pinned                                              | その `head` が pinned args の revision に含まれる                                                                                                                                                       | 4.4 のラベル非表示とは独立に pinned 対象    |
| TC-312  | detached 1 件を返す worktree query 込みで `getCommits()` を実行                                              | Normal - response collection shape                                         | 返却値の `worktrees` が `branches` と `detached` を持つ collection で、porcelain 由来の値と `toEqual` で一致する                                                                                        | 応答 field の形状                           |

### 失敗源インベントリ（include-or-justify）— Feature 052 追加分（S47）

| 失敗源                                                                    | 対応ケースまたは除外理由                                                                                                                     |
| ------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| worktree 取得失敗を `{}` のまま返して型と値が崩れる                       | TC-293、TC-309                                                                                                                               |
| worktree 取得を待たずに pinned query の引数を組む                         | TC-294                                                                                                                                       |
| detached が空でも pinned query を spawn する                              | TC-295                                                                                                                                       |
| 同一 head を重複して Git 引数へ渡す                                       | TC-296                                                                                                                                       |
| pinned query に `--max-count` / order / branch 系 flag を混ぜる           | TC-297、TC-300                                                                                                                               |
| author filter を pinned へ渡さない、escape 規則がずれる                   | TC-298、TC-299                                                                                                                               |
| 未検証の hash を Git 引数へ渡す                                           | TC-301                                                                                                                                       |
| 通常窓外の HEAD を追加しない（通常 log の revision へ足すだけで済ませる） | TC-302                                                                                                                                       |
| 通常列に存在する hash を重複追加する                                      | TC-303、TC-304                                                                                                                               |
| pinned を件数・順序判定へ混ぜて通常表示を壊す                             | TC-305、TC-306、TC-307                                                                                                                       |
| pinned 失敗で通常結果まで失う                                             | TC-308                                                                                                                                       |
| 統合を node 化・stash・ref 付与より後に行う                               | TC-310                                                                                                                                       |
| detached main を pinned 対象から外す                                      | TC-311                                                                                                                                       |
| 応答 field の形状が collection になっていない                             | TC-312                                                                                                                                       |
| 境界値（0 / empty）                                                       | TC-295（detached 0 件）、TC-293 / TC-309（空 collection）                                                                                    |
| 境界値（maximum / +/-1）                                                  | TC-305（`maxCommits + 1` 件）、TC-306（`maxCommits` 以下）                                                                                   |
| 境界値（minimum / NULL）                                                  | excluded(`maxCommits` の下限は既存 S21 / `web/main-test/07-load-count-01.md` の責務。`worktrees` は必須の collection で `null` を取り得ない) |
| 外部依存の失敗（Git 実行）                                                | TC-293、TC-308、TC-309                                                                                                                       |
| 例外送出                                                                  | excluded(`spawnGit()` の errorValue fallback により本経路は throw しない。fallback 値は TC-293 / TC-308 / TC-309 で検証)                     |
| porcelain レコードの分類誤り                                              | excluded(`src/worktree-test.md` S2 の責務)                                                                                                   |
| hash 値域そのものの誤り                                                   | excluded(`src/utils-test.md` S7 の責務)                                                                                                      |
| ラベル描画・event 抑止                                                    | excluded(`web/main-test/01-rendering-02.md` S48 の責務)                                                                                      |

**失敗カテゴリ網羅（diversity floor）**:

- Validation: TC-300、TC-301
- Exception: TC-293、TC-308
- External: TC-309
- Boundary: TC-295、TC-296、TC-299、TC-303、TC-304、TC-305、TC-306
- Type: excluded(引数と戻り値の型契約は `src/types-test.md` S7 の責務。runtime の値検証は TC-301 で担保)

**失敗系/正常系比（煙感知器）**: 正常系 9 件（TC-292、TC-294、TC-297、TC-298、TC-302、TC-307、TC-310、TC-311、TC-312）、失敗系 12 件（TC-293、TC-295、TC-296、TC-299、TC-300、TC-301、TC-303〜TC-306、TC-308、TC-309）。比 1.33 で近接（差 1 以内）ではないため、インベントリ再導出は不要と判断した。
