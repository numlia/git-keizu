# テスト観点表: src/dataSource.ts

> Source: `src/dataSource.ts`
> Generated: 2026-09-08T00:00:00Z
> Language: TypeScript
> Test Framework: Vitest
> Responsibility: file-history

## S49: getFileHistory() Git 引数・呼出回数・失敗分離

> Origin: Feature 055-07 (light-spec-plan)
> Added: 2026-09-08
> Status: active
> Supersedes: -
> Signature: `public async getFileHistory(repo: string, anchorHash: string, filePath: string): Promise<FileHistoryEntry[] | null>`
> Target Path: `src/dataSource.ts`（`getFileHistory()`。`getBranchCleanup()` の直後。実装後に行範囲へ更新）
> Test File: `tests/src/dataSource.test.ts`

`runGitQuery()` を lineage / merge / ancestry の最大 3 回呼び、`src/fileHistory.ts` の純関数へ委譲する orchestration の観点（対応プラン §3.7 / §4 Task 3）。parse と規則の分岐は `src/fileHistory-test.md` S1〜S8 の責務で、本表は `cp.spawn` mock の args・call count・失敗段階ごとの `null` だけを対象にする。args の期待値は `buildLineageQueryArgs()` 等の戻り値と `toEqual` で比較する。

| Case ID | Input / Precondition                                                                        | Perspective (Normal / Validation / Exception / External / Boundary / Type) | Expected Result                                                                                                                                                  | Notes                                  |
| ------- | ------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- |
| TC-327  | `getFileHistory(repo, "zz", "src/a.txt")`                                                   | Validation - 不正 anchorHash                                               | `cp.spawn` の call count が 0 で戻り値が `null`                                                                                                                  | `isValidCommitHash()` 偽               |
| TC-328  | `getFileHistory(repo, <valid hash>, "a/../b")`                                              | Validation - 不正 path                                                     | `cp.spawn` の call count が 0 で戻り値が `null`                                                                                                                  | `normalizeFileHistoryPath()` が `null` |
| TC-329  | 有効 hash と `"src/a.txt"`、lineage / merge の stdout が空                                  | Normal - lineage query の args                                             | 1 回目の `cp.spawn` の args が `buildLineageQueryArgs(anchorHash, "src/a.txt")` の戻り値と `toEqual` で一致し、options の `cwd` が repo、`env.LC_ALL` が `"C"`   | shell を経由しない                     |
| TC-330  | `filePath` が `"src\\a.txt"`                                                                | Normal - 正規化後 path を渡す                                              | 1 回目の args の末尾要素が `"src/a.txt"`                                                                                                                         | 正規化は Git 起動前                    |
| TC-331  | lineage stdout が空、merge stdout も空                                                      | Boundary - 履歴なし                                                        | `cp.spawn` の call count が 2 で、2 回目の args が `buildMergeQueryArgs(anchorHash, null, ["src/a.txt"])` の戻り値と `toEqual`、戻り値が `[]`（`null` ではない） | lineage 0 件でも merge 実行（F1）      |
| TC-332  | lineage stdout が `D` 停止する fixture（boundary あり）                                     | Normal - boundary 付き merge args                                          | 2 回目の args に `^<boundaryHash>` が 1 要素として含まれ、`buildMergeQueryArgs(anchorHash, boundary.hash, historicalPaths)` の戻り値と `toEqual`                 | -                                      |
| TC-333  | lineage stdout に rename `R legacy→current` を含む fixture                                  | Normal - historical path 集合の受け渡し                                    | 2 回目の args の `"--"` 以降が `["src/current-name.txt", "src/legacy-name.txt"]` と `toEqual`                                                                    | 挿入順                                 |
| TC-334  | lineage の `cp.spawn` が exit 1                                                             | External - lineage 失敗                                                    | `cp.spawn` の call count が 1 で戻り値が `null`                                                                                                                  | merge を起動しない                     |
| TC-335  | lineage stdout が `"x"` 始まりの malformed                                                  | Type - lineage parse 失敗                                                  | `cp.spawn` の call count が 1 で戻り値が `null`                                                                                                                  | `parseFileHistoryRecords()` が `null`  |
| TC-336  | lineage stdout が tracked path と無関係な record だけ                                       | Validation - lineage 規則失敗                                              | `cp.spawn` の call count が 1 で戻り値が `null`                                                                                                                  | `applyLineageRules()` が `null`        |
| TC-337  | merge の `cp.spawn` が exit 1                                                               | External - merge 失敗                                                      | `cp.spawn` の call count が 2 で戻り値が `null`（lineage の部分結果を返さない）                                                                                  | 部分結果禁止                           |
| TC-338  | merge stdout が malformed                                                                   | Type - merge parse 失敗                                                    | `cp.spawn` の call count が 2 で戻り値が `null`                                                                                                                  | -                                      |
| TC-339  | merge stdout に親 1 件の record を含む                                                      | Validation - merge 規則失敗                                                | `cp.spawn` の call count が 2 で戻り値が `null`                                                                                                                  | `applyMergeRules()` が `null`          |
| TC-340  | merge stdout に誕生 merge 候補 1 件、lineage entry 1 件以上（総数 2 件以上）、boundary あり | Normal - ancestry query の args                                            | 3 回目の `cp.spawn` の args が `buildAncestryQueryArgs(anchorHash, boundary.parentHashes)` の戻り値と `toEqual` で一致し、`^` 要素が boundary 親の数だけある     | 実行条件を満たす                       |
| TC-341  | 候補 1 件・総数 2 件以上・boundary なし（末尾到達）                                         | Boundary - boundary なしの ancestry args                                   | 3 回目の args が `["rev-list", "--parents", anchorHash]` と `toEqual`（`^` 要素 0 件）                                                                           | CE6 形                                 |
| TC-342  | lineage 0 件、merge が誕生 merge `A` 1 件（総数 1 件）                                      | Boundary - 候補ありでも総数 1 件                                           | `cp.spawn` の call count が 2 で戻り値が 1 件（`type: "A"`、`isMerge: true`）                                                                                    | CE1 / CE2 形                           |
| TC-343  | 候補 0 件で総数 2 件以上                                                                    | Boundary - 候補 0 件                                                       | `cp.spawn` の call count が 2 で戻り値が lineage + merge の連結と `toEqual`                                                                                      | `similar` 形                           |
| TC-344  | ancestry の `cp.spawn` が exit 1                                                            | External - ancestry 失敗                                                   | `cp.spawn` の call count が 3 で戻り値が `null`                                                                                                                  | 部分結果禁止                           |
| TC-345  | ancestry stdout に `zz` token を含む                                                        | Type - ancestry parse 失敗                                                 | `cp.spawn` の call count が 3 で戻り値が `null`                                                                                                                  | `parseAncestryGraph()` が `null`       |
| TC-346  | CE3 形の lineage / merge / ancestry stdout                                                  | Normal - 合成結果の返却                                                    | 戻り値が `resolveFileHistoryEntries()` の結果（2 件。旧 `A` と delete merge を含まない）と `toEqual`                                                             | 除外の end-to-end                      |

## S50: getFileHistory() real repository fixture の期待 entry

> Origin: Feature 055-07 (light-spec-plan)
> Added: 2026-09-08
> Status: active
> Supersedes: -
> Signature: `public async getFileHistory(repo: string, anchorHash: string, filePath: string): Promise<FileHistoryEntry[] | null>`
> Target Path: `src/dataSource.ts`（`getFileHistory()`。実装後に行範囲へ更新）
> Test File: `tests/src/dataSource.git.test.ts`

`tests/src/dataSource.git.test.ts` の pattern（`execFileSync("git", args)`、`git init -b main`、`merge.ff=false`、固定 author / committer date）で確定仕様 3.2.1 の 6 fixture と CE1〜CE7 を構築し、戻り値を確定仕様 8.1 と「実測根拠（2026-09-08）」表の期待 entry へ突き合わせる観点。subject は `git log --format=%s -1 <hash>` で解決し、entry は順序を問わず集合として比較する（`*` は `isMerge: true`）。

| Case ID | Input / Precondition                                                                                            | Perspective (Normal / Validation / Exception / External / Boundary / Type) | Expected Result                                                                                                                                                                                                                                                                                              | Notes                                                                                                             |
| ------- | --------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------- |
| TC-347  | `similar`、anchor `fix: update file after merge`、path `src/current-name.txt`                                   | Normal - rename と merge を含む典型                                        | 6 件: fix `M`、`merge: rename feature` `R`\*、`feat: update renamed file` `M`、`refactor: rename tracked file` `R`（`historicalPath: "src/current-name.txt"`、`oldFilePath: "src/legacy-name.txt"`）、`feat: update legacy file` `M`（`historicalPath: "src/legacy-name.txt"`）、`base: add legacy file` `A` | merge の score は比較しない                                                                                       |
| TC-348  | `similar`、anchor `feat: recreate same path`（HEAD）                                                            | Boundary - 再作成 HEAD                                                     | 1 件: `feat: recreate same path` `A` / `isMerge: false`。`refactor: delete tracked file` より古い hash を含まない                                                                                                                                                                                            | birth boundary                                                                                                    |
| TC-349  | `similar`、anchor `refactor: delete tracked file`                                                               | Boundary - anchor 自身の D                                                 | 7 件: anchor の `D` と TC-347 の 6 件                                                                                                                                                                                                                                                                        | -                                                                                                                 |
| TC-350  | `dissimilar`、anchor `fix: update file after merge`                                                             | Normal - rename 未検出 merge                                               | TC-347 と同じ 6 subject で、`merge: rename feature` だけが `A`\*（`oldFilePath === newFilePath === "src/current-name.txt"`）                                                                                                                                                                                 | 第 1 親差分で rename 不検出                                                                                       |
| TC-351  | `dissimilar`、anchor `feat: recreate same path`                                                                 | Boundary - 再作成 HEAD                                                     | 1 件: `feat: recreate same path` `A`                                                                                                                                                                                                                                                                         | -                                                                                                                 |
| TC-352  | `conflict`、anchor `fix: update file after merge`                                                               | Normal - 両親に差分を持つ merge                                            | 7 件: fix `M`、merge `A`\*、`feat: update renamed file` `M`、`refactor: rename tracked file` `R`、`feat: main edits legacy before merge` `M`（`historicalPath: "src/legacy-name.txt"`）、`feat: update legacy file` `M`、`base: add legacy file` `A`                                                         | merge は `separate` で 2 record → 1 entry                                                                         |
| TC-353  | `conflict`、anchor `feat: recreate same path`                                                                   | Boundary - 再作成 HEAD                                                     | 1 件: `feat: recreate same path` `A`                                                                                                                                                                                                                                                                         | -                                                                                                                 |
| TC-354  | `conflict-similar`、anchor `fix: update file after merge`                                                       | Normal - rename 検出と conflict 解消が同じ merge                           | 6 件: fix `M`、merge `R`\*、`feat: update renamed file` `M`、`refactor: rename tracked file` `R`、`feat: main edits legacy` `M`（`historicalPath: "src/legacy-name.txt"`）、`base: add legacy file` `A`                                                                                                      | -                                                                                                                 |
| TC-355  | `unrelated-merge`、anchor `fix: update file after merge`                                                        | Validation - 無関係 merge を返さない                                       | 5 件: fix `M`、`merge: rename feature` `R`\*、`feat: update renamed file` `M`、`refactor: rename tracked file` `R`、`base: add legacy file` `A`。`merge: unrelated other` の hash を含まない                                                                                                                 | pathspec で落ちる                                                                                                 |
| TC-356  | `evil`、anchor `edit new f`、path `src/f.txt`                                                                   | Validation - 削除済み path を merge が再作成                               | 2 件: `edit new f` `M`、`evil merge: creates f` `A`\*。`old incarnation: add f` / `old incarnation: edit f` / `old incarnation: delete f` を含まない                                                                                                                                                         | ancestry query 実行                                                                                               |
| TC-357  | CE1: `evil`、anchor `evil merge: creates f`                                                                     | Boundary - 誕生 merge 自身が anchor                                        | 1 件: `evil merge: creates f` `A`\*                                                                                                                                                                                                                                                                          | lineage 0 件でも merge 実行（F1）                                                                                 |
| TC-358  | CE2: `init` → `x: unrelated` → `main: unrelated` → merge 中に `src/f.txt` 作成、anchor `birth merge: creates f` | Boundary - path が過去に存在しない誕生 merge                               | 1 件: `birth merge: creates f` `A`\*                                                                                                                                                                                                                                                                         | 総数 1 件で ancestry 省略                                                                                         |
| TC-359  | CE3: merge で削除・merge で再作成、anchor `edit new f`                                                          | Normal - 誕生 merge による旧 incarnation 除外                              | 2 件: `edit new f` `M`、`recreate merge: creates f` `A`\*。`old incarnation: add f` と `delete merge: removes f` を含まない                                                                                                                                                                                  | 除外あり                                                                                                          |
| TC-360  | CE4: conflict を main 側内容で解消した `merge: keep main`、anchor `edit f after merge`                          | Normal - 第 1 親と TREESAME な merge                                       | 5 件: `edit f after merge` `M`、`merge: keep main` `M`\*（`isMerge: true`）、`main: edit f` `M`、`feature: edit f` `M`、`base: add f` `A`                                                                                                                                                                    | fixture 上 `git diff <merge>^ <merge> --name-status` は空（CDV 注記は `web/main-test/10-file-history-01.md` S50） |
| TC-361  | CE5: rename 後の旧 path を別ファイルが merge で再利用、anchor `fix: update current after reuse`                 | Validation - 無効候補で除外なし                                            | 7 件: fix `M`、`merge: rename feature` `R`_、`feat: update renamed file` `M`、`refactor: rename tracked file` `R`、`feat: update legacy file` `M`、`base: add legacy file` `A`、`reuse merge: creates legacy path` `A`_                                                                                      | 候補は無効                                                                                                        |
| TC-362  | CE6: merge で誕生後に通常 rename、anchor `fix: edit current`、path `src/current-name.txt`                       | Normal - rename 元 path の有効候補                                         | 3 件: `fix: edit current` `M`、`refactor: rename tracked file` `R`、`birth merge: creates legacy` `A`\*（`historicalPath: "src/legacy-name.txt"`）                                                                                                                                                           | boundary なし                                                                                                     |
| TC-363  | CE7: CE3 と同じ構築で anchor `recreate merge: creates f`                                                        | Boundary - anchor 自身が有効候補                                           | 1 件: `recreate merge: creates f` `A`\*。`old incarnation: add f` / `old incarnation: edit f` / `delete merge: removes f` を含まない                                                                                                                                                                         | 候補自身は残す                                                                                                    |

### 失敗源インベントリ（include-or-justify）— Feature 055-07 追加分（S49〜S50）

| 失敗源                                                | 対応ケースまたは除外理由                                                                                                                                                  |
| ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 不正入力で Git を起動する                             | TC-327、TC-328                                                                                                                                                            |
| args の不一致（quote・順序・`^` の有無・path 集合）   | TC-329〜TC-333、TC-340、TC-341                                                                                                                                            |
| Git 失敗（exit 非 0）を空成功として返す               | TC-334、TC-337、TC-344                                                                                                                                                    |
| parse 不能・規則失敗で部分結果を返す                  | TC-335、TC-336、TC-338、TC-339、TC-345                                                                                                                                    |
| lineage 0 件で merge query を省略する（旧仕様）       | TC-331、TC-342、TC-357                                                                                                                                                    |
| ancestry query の過剰実行・省略誤り                   | TC-340〜TC-343                                                                                                                                                            |
| 旧 incarnation の混入・無関係 merge の混入            | TC-348、TC-351、TC-353、TC-355、TC-356、TC-359、TC-361、TC-363                                                                                                            |
| merge の位置・type の取り違え                         | TC-347、TC-350、TC-352、TC-354、TC-360、TC-362                                                                                                                            |
| 境界値（0 / minimum / maximum / +/-1 / empty / NULL） | spawn 0 回: TC-327、TC-328。空 stdout: TC-331。総数 1 件: TC-342、TC-357、TC-358。候補 0 件: TC-343。anchor 自身: TC-349、TC-363。maximum: excluded(entry 数に上限がない) |
| 外部依存×失敗モード                                   | TC-334、TC-337、TC-344（`cp.spawn` の exit 非 0）。spawn の `error` event は既存 `runGitQuery()` の契約（`04-spawn-refname-diff-01.md`）で担保済みのため excluded         |
| 例外・エラー経路                                      | excluded(`getFileHistory()` は throw せず `null` へ写像する。`null` の段階別分岐は TC-334〜TC-339、TC-344、TC-345 で固定)                                                 |
| stale response / anchor 不在 / DOM 再生成 / unload    | excluded(webview owner `web/fileHistory-test.md` の責務。DataSource は request ごとに独立)                                                                                |

**失敗カテゴリ網羅（diversity floor）**:

- Validation: TC-327、TC-328、TC-336、TC-339、TC-355、TC-356、TC-361
- Exception: excluded(throw 経路なし。上表のとおり)
- External: TC-334、TC-337、TC-344
- Boundary: TC-331、TC-341〜TC-343、TC-348、TC-349、TC-351、TC-353、TC-357、TC-358、TC-363
- Type: TC-335、TC-338、TC-345
- Normal: TC-329、TC-330、TC-332、TC-333、TC-340、TC-346、TC-347、TC-350、TC-352、TC-354、TC-359、TC-360、TC-362

**失敗系/正常系比（煙感知器）**: 正常系13件、失敗系24件。orchestration の失敗源（不正入力・3 段階の Git 失敗・parse / 規則失敗・実行条件・fixture の反例）を上表で列挙し、比 1.8 倍はインベントリから自然に導かれた値であることを確認した。比率合わせのためのケース追加・削除は行わない。
